# TASK: Antigravity Audit & Verification — Series Scaffolding & Dynamic Compilation

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## TARGET WORKSPACES: `packages/001.lore`, `series/`, `compiled/`, and `apps/995.library`

You are an expert systems architect and autonomous QA verification
engineer[cite: 4]. Your objective is to audit and prove that the new **Series
Lifecycle Engine** in `packages/001.lore` is completely operational[cite: 3].

Specifically, verify that:

1. The **Immutable Runner Boundary** of `apps/995.library/` remains strictly
   untouched[cite: 1, 3].
2. `CREATE SERIES` interactively prompts for a series name, generates a valid
   `series/<slug>/` hierarchy with Byte-0 seed files, and pre-compiles at
   $T_0 = \$0.00$[cite: 3, 8].
3. `COMPILE LORE INSTANCE` dynamically sweeps `<rootDir>/series/`, presents a
   curses selection panel, and only compiles the selected series[cite: 3].
4. Compilation emits dual artifacts to `<rootDir>/compiled/<slug>/`
   (`bible-state.json` and `bible-state_<TIMESTAMP>.json`), both tracked by
   Git[cite: 3].

Do not modify functional code without reporting your findings first[cite: 4].
Run the required terminal commands, inspect the source files, and deliver an
objective **PASSED / FAILED** verification report[cite: 4].

---

### Phase 1: Boundary & Workspace Scope Audit

1. **Immutable Boundary Fence:**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** Returns zero lines[cite: 1, 3]. No files created,
     modified, or deleted inside `apps/995.library/`[cite: 1, 3].

2. **Git Hygiene (Option 1 Compliance):**
   - Inspect root `.gitignore`[cite: 3].
   - Confirm that timestamped files (`compiled/**/*_*.json`) are **NOT ignored**
     (ensure they are tracked in Git)[cite: 3].

---

### Phase 2: Static Code & Terminal Wiring Audit

1. **Action & Reducer Wiring (`packages/001.lore/01.series.unit/`):**
   - Check `series.action.ts`: verify `CREATE_SERIES` action and `CreateSeries`
     class are declared[cite: 3].
   - Check `series.buzzer.ts`: verify `createSeries` is exported[cite: 3].
   - Check `series.reduce.ts`: verify `case Act.CREATE_SERIES` delegates to
     `Buzz.createSeries`[cite: 3].

2. **Scaffolding Logic
   (`packages/001.lore/01.series.unit/buz/series.buzz.ts`):**
   - Inspect `createSeries`:
     - Confirm slug sanitization (kebab-case, stripping invalid
       characters)[cite: 3].
     - Confirm creation of subdirectories: `characters/`, `grievances/`,
       `locations/`, `possessions/`[cite: 3].
     - Confirm creation of `series.config.json` with `modal_parameters`[cite:
       3].
     - Confirm seed Markdown files (`central_hub.md`, `founders_token.md`,
       `protagonist.md`, `territorial_claim.md`) begin YAML frontmatter strictly
       at byte 0 (`---`)[cite: 3].
     - Confirm immediate zero-defect pre-compilation via `compileLoreInstance`
       before returning[cite: 3].

3. **Parameterized Compiler Dispatch
   (`packages/001.lore/00.lore.unit/buz/lore.buzz.ts`):**
   - Inspect `compileLore`:
     - Confirm `seriesSlug` is read dynamically from `bal.src` rather than
       hardcoded to `under-the-floorboards`[cite: 3].
     - Confirm target directory resolves to `<rootDir>/series/<seriesSlug>` and
       output targets `<rootDir>/compiled`[cite: 3].

4. **Terminal Menu Integration
   (`packages/001.lore/98.menu.unit/buz/00.menu.buzz.ts`):**
   - Inspect `updateMenu`:
     - Confirm choice list contains: `'COMPILE LORE INSTANCE'`,
       `'CREATE SERIES'`, `'TEST SERIES'`, `'ROOT MENU'`[cite: 3].
     - **`CREATE SERIES` Flow:** Dispatches `[Grid action] Update Grid` and
       `[Open action] Open Input` to capture the name, then hunts
       `ActSer.CREATE_SERIES`[cite: 3].
     - **`COMPILE LORE INSTANCE` Flow:** Reads `<rootDir>/series/`, builds a
       directory list, dispatches `[Open action] Open Choice` with a `'CANCEL'`
       fallback, and hunts `ActLor.COMPILE_LORE` with the selected slug[cite:
       3].

---

### Phase 3: Automated Test Suite Execution

Execute the full package test gauntlet via terminal and capture the output:

```bash
# 1. Typecheck the package
npm run build --workspace=@camp_candor/001.lore

# 2. Run package Vitest suite
npm run test:lore

```

**Pass Conditions:**

- `tsc -b` exits with code 0.

- `test/compiler.test.ts` passes clean compilation and DAG cycle checks.

- `test/series.test.ts` passes the series lifecycle test (proving
  `CREATE_SERIES` scaffolds compliant files and `COMPILE_LORE` seals them with
  dual outputs).

---

### Phase 4: Live Functional Proof (Simulation Smoke Test)

Run an end-to-end programmatic verification to prove the series creation and
dual-emission pipeline works without errors:

1. **Execute Headless Scaffold & Compile Probe:** Run a short Node/tsx script or
   interactive test to execute the following sequence:

- Hunt `ActSer.CREATE_SERIES` with `src: "audit-verification-series"`.

- Confirm `series/audit-verification-series/` is created on disk.

- Verify that `series/audit-verification-series/characters/protagonist.md`
  starts with `---` at byte 0.

- Hunt `ActLor.COMPILE_LORE` with `src: "audit-verification-series"`.

- Verify output in `compiled/audit-verification-series/`:
- `bible-state.json` exists.

- `bible-state_<TIMESTAMP>.json` exists and uses Windows-safe naming (no
  colons).

- Both files share the identical `state_hash`.

2. **Teardown Test Artifacts:**

- Clean up `series/audit-verification-series/` and
  `compiled/audit-verification-series/`.

---

### Output Deliverable Format

Structure your findings in the following format:

1. **Terminal Command Log:**

- Exact commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each capability as **PASSED** or **FAILED**:
- `Immutable Runner Fence (apps/995.library untouched)`

- `CREATE SERIES Input Panel Integration`

- `Series Directory & Byte-0 Seed Generation`

- `T0 Pre-Compilation Check ($0.00 compute)`

- `Dynamic Series Selection Sub-Menu in COMPILE LORE`

- `Dual-Emission Output in compiled/<seriesSlug>/`

- `Windows-Safe Sanitized Timestamp Filenames`

- `Package Test Gauntlet (npm run test:lore)`

3. **Identified Violations & Immediate Patches:**

- If any check fails, state the exact file path and code patch required to
  resolve it.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Series Lifecycle & Dynamic Compiler Certified)**
  or **BLOCKED (Audit Gate Tripped)**.

```

```
