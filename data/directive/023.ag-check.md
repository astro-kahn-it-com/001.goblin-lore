# TASK: Antigravity Audit & Verification — Golden-File Failure Gauntlet & Full Engine Integrity

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## AUDIT TARGETS: `packages/001.lore`, `series/`, `compiled/`, and `apps/995.library`

You are an expert systems architect and autonomous QA verification
engineer[cite: 14]. Your mission is to execute a forensic audit across
`packages/001.lore` to certify that the **Golden-File Failure Gauntlet**, **Pass
1 & Pass 2 Invariant Rules**, **Byte-0 Frontmatter Laws**, and **Dual-Emission
Compilation Engine** are 100% operational, fail-closed, and
non-discriminating[cite: 5, 7, 8].

Under the system's **Universal Negative-Control Law**, a validation gate that
has never been observed failing has not been proven to work[cite: 5, 7]. You
must prove that the compiler reliably trips on malformed inputs and executes
without side effects[cite: 5, 7].

Do not modify functional code without reporting your findings first[cite: 14].
Execute terminal validation commands directly, inspect files, and deliver an
objective **PASSED / FAILED** verification report[cite: 14].

---

### Phase 1: Boundary & Git Hygiene Audit

1. **Harness Boundary Confirmation (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** Confirm that `apps/995.library/995.library/**` has zero
     modifications[cite: 1, 4].
   - Confirm that the **only** permitted modification path within
     `apps/995.library/` is `apps/995.library/run.ts` (strictly for dynamic
     package loader fallbacks and CLI initialization)[cite: 1, 4].

2. **Git Hygiene & Dual-Emission Tracking:**
   - Inspect root `.gitignore`[cite: 4, 18].
   - Assert that `compiled/**/*_*.json` is **NOT present** in `.gitignore`[cite:
     4].
   - Run:
     ```bash
     git check-ignore compiled/under-the-floorboards/bible-state_*.json
     ```
   - **Pass Condition:** Returns empty with exit code 1, confirming that both
     canonical HEAD (`bible-state.json`) and timestamped historical snapshots
     are tracked in Git[cite: 4, 7].

---

### Phase 2: Static Golden Failure Fixtures Inspection

Inspect `packages/001.lore/test/fixtures/failures/` to verify that all failure
fixtures and their `expected_error.json` manifests exist on disk[cite: 4, 5]:

1. **Pass 1 Physical & Encoding Fixtures:**
   - `pass1_err_byte0_bom`:
     - Inspect `characters/bad_bom.md` using byte/hex inspection[cite: 4].
       Confirm byte 0 begins with UTF-8 BOM (`0xEF 0xBB 0xBF` / `\uFEFF`) before
       `---`[cite: 4].
     - Confirm `expected_error.json` targets `BYTE_ZERO_VIOLATION` with regex
       matching `does not begin with YAML frontmatter at byte 0`[cite: 4].
   - `pass1_err_byte0_whitespace`:
     - Inspect `characters/leading_space.md`. Confirm byte 0 is a newline
       (`0x0A` or `0x0D 0x0A`) before `---`[cite: 4].
     - Confirm `expected_error.json` targets `BYTE_ZERO_VIOLATION`[cite: 4].

2. **Pass 1 Scalar Mathematical Invariant Fixtures:**
   - `pass1_err_scalar_overflow`:
     - Inspect `grievances/overflow.md`. Confirm
       `intensity_envelope: [0, 10001]`[cite: 4].
     - Confirm `expected_error.json` targets
       `Number must be less than or equal to 10000`[cite: 4].
   - `pass1_err_float_basis_points`:
     - Inspect `grievances/float.md`. Confirm floating-point numbers in
       `intensity_envelope: [100.5, 5000.25]`[cite: 4].
     - Confirm `expected_error.json` targets
       `Expected integer, received float`[cite: 4].
   - `pass1_err_escalation_ceiling`:
     - Inspect `characters/ceiling_overflow.md`. Confirm
       `escalation_ceiling: 6`[cite: 4].
     - Confirm `expected_error.json` targets
       `Number must be less than or equal to 5`[cite: 4].

3. **Pass 2 Referential Integrity & DAG Fixtures:**
   - `pass2_err_dangling_location`: Character referencing missing
     `loc_abyss`[cite: 4].
   - `pass2_err_dangling_possession`: Character equipping missing
     `item_phantom_blade` in `held`[cite: 4].
   - `pass2_err_self_leverage`: Character declaring `leverage_strings` against
     itself[cite: 4].
   - `pass2_err_cyclic_grievance`: Circular escalation loop between `grv_g1` and
     `grv_g2`[cite: 4].

---

### Phase 3: Automated Test Execution & Deletion Principle

Execute the complete Vitest test suite and audit failure behaviors:

```bash
# 1. Typecheck the package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the full unit and golden-failure test suites
npm run test:lore

```

**Pass Conditions:**

1. `packages/001.lore/test/compiler.test.ts` passes all 9 inline referential
   integrity tests.

2. `packages/001.lore/test/golden_failures.test.ts` passes all 9 static golden
   fixture cases.

3. **Zero-Emission Invariant / Deletion Principle Verification:**

- Confirm that running `golden_failures.test.ts` left **zero residue**: no
  `compiled/` directory or partial `.json` files exist inside any folder under
  `packages/001.lore/test/fixtures/failures/`.

4. `packages/001.lore/test/series.test.ts` passes all series lifecycle and
   dynamic compilation assertions.

---

### Phase 4: Dynamic Series Creation & Parameterized Dual-Emission Probe

Execute an end-to-end headless probe verifying dynamic series scaffolding,
hermetic isolation, and dual-emission output:

1. **Scaffold Probe:**

- Run a Node/tsx script or interactive CLI probe dispatching
  `ActSer.CREATE_SERIES` with `src: "antigravity-probe"`.

- Confirm `series/antigravity-probe/` is created with:
- `series.config.json`

- `characters/protagonist.md` (starts with `---` at byte 0)

- `locations/central_hub.md` (starts with `---` at byte 0)

- `possessions/founders_token.md` (starts with `---` at byte 0)

- `grievances/territorial_claim.md` (starts with `---` at byte 0)

- Confirm the probe logged: `>> [PRE-COMPILED] Hash: ...` confirming
  $T_0 = \$0.00$ pre-compilation passed.

2. **Cross-Series Hermetic Pointer Check:**

- In `series/antigravity-probe/characters/protagonist.md`, inject an
  un-scaffolded possession:

```yaml
held: ['item_silver_spoon']
```

_(Note: `item_silver_spoon` exists in `under-the-floorboards`, but NOT in
`antigravity-probe`)_.

- Execute `compileLoreInstance` on `series/antigravity-probe`.

- **Assert Hard Halt:** Must throw:

```text
[PASS 2 ERROR] Character 'char_antigravity-probe_protagonist' equips missing possession in 'held': 'item_silver_spoon'

```

proving hermetic namespace isolation across series.

3. **Dual-Emission Output Verification:**

- Revert the deliberate error and compile `series/antigravity-probe` cleanly.

- Inspect `<rootDir>/compiled/antigravity-probe/`:
- Assert `bible-state.json` exists.

- Assert `bible-state_<TIMESTAMP>.json` exists and contains no colons in the
  filename (`YYYY-MM-DDTHH-mm-ssZ`).

- Assert both files are byte-for-byte identical (NFC normalized) and share the
  exact same `_meta.state_hash`.

- Assert the calculated hash contains zero ambient clock data.

4. **Teardown Probe:**

- Clean up `series/antigravity-probe/` and `compiled/antigravity-probe/`.

---

### Phase 5: CI Zero-Drift Canonical Tripwire

1. **Audit Script Target:**

- Confirm root `package.json` contains:

```json
"audit:lore": "npm run compile:lore && git diff --exit-code compiled/under-the-floorboards/bible-state.json"

```

2. **Tripwire Execution:**

- Run:

```bash
npm run audit:lore

```

- **Pass Condition:** Exit code 0, confirming zero drift between authored
  Markdown in `series/under-the-floorboards/` and the committed canonical HEAD
  snapshot in `compiled/under-the-floorboards/bible-state.json`.

---

### Output Deliverable Format

Structure your findings in the following format:

1. **Terminal Command Log:**

- Commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each capability as **PASSED** or **FAILED**:

- `Harness Boundary Confirmation (apps/995.library/995.library/** untouched)`

- `Sole Modification Exception Verified (apps/995.library/run.ts only)`

- `Git Tracking Policy (compiled/**/*_*.json not ignored)`

- `Pass 1 Byte-0 Golden Controls (BOM & leading whitespace blocked)`

- `Pass 1 Scalar Mathematical Bounds (Overflow, floats, ceilings blocked)`

- `Pass 2 Referential Integrity & Dangling Pointers Blocked`

- `Pass 2 Self-Debt & Self-Adjacency Blocked`

- `Pass 2 DFS Escalation Graph Acyclicity Blocked`

- `Zero-Emission Invariant / Deletion Principle (No residue on failure)`

- `Hermetic Series Namespace Isolation Confirmed`

- `Dual-Emission Snapshots (HEAD + Windows-safe timestamp)`

- `Zero-Drift Tripwire Pass (npm run audit:lore)`

3. **Identified Violations & Immediate Patches:**

- If any check fails, state the exact file path, line number, and provide the
  exact patch required to resolve it.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Golden Failure Gauntlet Certified at $T_0$)** or
  **BLOCKED (Audit Gate Tripped)**.

```

```
