# TASK: Antigravity Audit & Verification — Lexical Verb Gate & Advisory Linter Battery

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## AUDIT TARGETS: `packages/001.lore/src/linters`, `packages/001.lore/schemas/ontology`, `packages/001.lore/linters`, `packages/001.lore/test/linters`, and `apps/995.library`

You are an expert systems architect and autonomous QA verification
engineer[cite: 5, 7]. Your mandate is to audit the newly implemented **Lexical
Verb Gate and Advisory Linter Battery** within `packages/001.lore` to certify
that the semantic gap between structured YAML frontmatter capability bounds and
narrative prose has been closed without compromising compiler purity[cite: 4].

Under the **Universal Negative-Control Law**, a gate that has never been
observed failing has not been proven to work[cite: 5, 7]. You must use your
terminal execution, file inspection, and static analysis capabilities to audit
the implementation, verify mathematical calibration, run regression suites, and
produce an objective **PASSED / FAILED** report[cite: 5, 7].

Do not modify functional code without reporting your findings first[cite: 5, 7].

---

### Phase 1: Boundary Integrity & Compiler Purity Audit

1. **Harness Boundary Confirmation (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** The command must return zero lines[cite: 1, 4]. Confirm
     that no files in `apps/995.library/` were touched during this
     implementation[cite: 1, 4].

2. **Core Compiler Purity Verification (`packages/001.lore/src/compiler.ts`):**
   - Inspect `packages/001.lore/src/compiler.ts`[cite: 4].
   - Confirm that **no natural language processing libraries, regex prose
     linters, or non-deterministic tokenizers** were imported or injected into
     `compileLoreInstance`[cite: 3, 4].
   - Confirm that Pass 1, Pass 2, and Pass 3 remain strictly mathematical and
     schema-bound, ensuring that the cryptographic state hash
     ($H_{\text{root}}$) remains untainted by NLP dependencies[cite: 3, 4].

---

### Phase 2: Static Code & Ontology Inspection

1. **Verb-Family Taxonomy
   (`packages/001.lore/schemas/ontology/verb_families.json`):**
   - Confirm the presence and valid JSON structure of `verb_families.json`[cite:
     4].
   - Verify that verb families (such as `LOCOMOTION_RAPID` and `BIMANUAL_GRIP`)
     define[cite: 4]:
     - `capability_required` (e.g., `bipedal_sprint`,
       `two_functional_arms`)[cite: 4].
     - `banned_if_conditions` (e.g., `stiff_left_knee`, `withered_left_arm`,
       `famished`)[cite: 4].
     - Explicit verb lists (`sprint`, `vault`, `climb`, `grip`, `embrace`)[cite:
       4].
     - `contextual_abstract_whitelist` containing cognitive/metaphorical direct
       objects (`fate`, `destiny`, `silence`, `memory`, `terror`)[cite: 4].

2. **Tier 1 Somatic Lexical Gate
   (`packages/001.lore/src/linters/lexicalGate.ts`):**
   - Verify `LexicalVerbGate` implementation[cite: 4]:
     - **Modal Negation Window:** Asserts that candidate verbs preceded by
       negative modifiers (`not`, `never`, `cannot`, `couldn't`, `unable`,
       `failed`, `without`) within a 5–6 token backward window are treated as
       non-breaches (valid depictions of physical trauma)[cite: 3, 4].
     - **Direct Object Abstraction Filter:** Asserts that candidate verbs
       governing abstract concepts on the whitelist bypass somatic flags
       (`Bog embraced his fate` passes; `Bog embraced the ladder` fails)[cite:
       4].
     - **Fail-Closed Ambiguity Quarantine:** Confirms that unresolvable pronouns
       or dialect contractions (`him`, `her`, `'im`, `ain't`, `someone`) set
       `isAmbiguous: true` and increment `quarantinedCount` rather than failing
       open[cite: 3, 4].
     - **Dual-Field AST Reconciliation:** Inspects `validateActionProseAST` to
       ensure that both self-reported AST actions and independently extracted
       prose actions are evaluated against the character's somatic masks[cite:
       3, 4].

3. **Tier 2 Advisory Craft Linter
   (`packages/001.lore/src/linters/craftLinter.ts`):**
   - Verify `CraftLinter` implementation[cite: 4]:
     - **Swain MRU Inversion Detector:** Flags instances where deliberate motor
       responses (`screamed`, `swung`, `bolted`) precede sensory stimuli
       (`heard`, `saw`, `felt`)[cite: 3, 4].
     - **Sentence Length Variance (SLV):** Calculates the standard deviation
       ($\sigma$) of sentence lengths across the prose, flagging flat cadences
       ($\sigma < 4.0$)[cite: 3, 4].
     - **Gesture Crutch Tracker:** Counts repetitive physical crutches
       (`nodded`, `furrowed`, `shrugged`, `blinked`, `frowned`) and records line
       coordinates[cite: 3, 4].
     - **RLHF Slop Filter:** Scans for AI cliches (`palpable`, `tapestry`,
       `delve`, `testament`, `cacophony`) and provides concrete
       replacements[cite: 3, 4].
     - **Strictly Advisory Authority:** Confirms that no method in `CraftLinter`
       throws a fatal process-halting exception[cite: 3, 4].

4. **Auteur Telemetry Engine
   (`packages/001.lore/src/linters/auteurTelemetry.ts`):**
   - Inspect `AuteurTelemetryEngine`[cite: 4]:
     - Distinguishes `OVERRIDE_INTENTIONAL` (dramatic craft choice) from
       `OVERRIDE_FALSE_POSITIVE` (parser defect)[cite: 4].
     - Confirms calculation of Rule Friction Density
       ($F_{\text{rule}} = \frac{\text{Total Overrides}}{\text{Total Evals}}$)
       and Defect Ratio
       ($R_{\text{defect}} = \frac{\text{False Positives}}{\text{Total Overrides}}$)[cite:
       4].
     - Confirms status classification: flags `HIGH_FRICTION` when
       $F_{\text{rule}} > 0.15$ ($N \ge 50$) and sets `QUARANTINED` when
       $R_{\text{defect}} > 0.25$ ($N \ge 30$)[cite: 4].

5. **Package Re-exports:**
   - Verify that `packages/001.lore/src/linters/index.ts` re-exports all three
     linter classes and interfaces[cite: 4].
   - Verify that `packages/001.lore/src/index.ts` re-exports
     `./linters/index.js`[cite: 4].

---

### Phase 3: Calibrated Benchmark Suite Inspection (`linters/evalSet.json`)

1. **Benchmark Size & Allocation:**
   - Inspect `packages/001.lore/linters/evalSet.json`[cite: 4].
   - Verify total labeled entries $N \ge 200$[cite: 4].
   - Confirm balanced category coverage across[cite: 4]:
     - `negation` (minimum $n \ge 50$)[cite: 4]
     - `affirmative` (minimum $n \ge 75$)[cite: 4]
     - `idiom` (minimum $n \ge 40$)[cite: 4]
     - `passive_causative` / `coreference` (minimum $n \ge 25$)[cite: 4]
   - Verify that each item specifies `id`, `category`, `text`, `character`,
     `banned_verb`, `expected_verdict` (`PASS` | `FAIL`), and `rationale`[cite:
     4].

---

### Phase 4: Automated Test Execution & Mathematical Proofs

Execute the full testing gauntlet from the monorepo root:

```bash
# 1. Typecheck the 001.lore package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the complete test suite including linter tests
npm run test:lore

```

**Pass Conditions:**

1. **Lexical Gate Unit Suite (`test/linters/lexicalGate.test.ts`):**

- Passes modal negation parsing (depiction of impairment passes).

- Passes abstract direct object whitelisting (`Bog embraced his fate` passes).

- Passes affirmative breach detection (`Bog gripped the brass spoon` fails).

- Passes fail-closed coreference quarantine (`Someone vaulted` flagged as
  ambiguous).

- Passes dual-field AST motor validation.

2. **Craft Linter Battery (`test/linters/craftLinter.test.ts`):**

- Correctly detects Swain MRU inversions.

- Measures sentence length variance ($\sigma < 4.0$ on flat cadence).

- Tracks gesture crutch densities with line coordinates.

- Flags RLHF slop tokens with editorial suggestions.

3. **Statistical Calibration Proofs (`test/linters/calibration.test.ts`):**

- Verifies that the aggregate non-breach pool satisfies the 95% Wilson Score
  Confidence Interval lower bound:

$$\text{Precision}_{\text{lower}} \ge 0.95 \quad (\alpha = 0.05)$$

- Verifies that the affirmative breach pool satisfies the 90% Wilson Score
  Confidence Interval lower bound:

$$\text{Recall}_{\text{lower}} \ge 0.90 \quad (\alpha = 0.05)$$

4. **Regression Invariant:**

- Confirm that `compiler.test.ts`, `golden_failures.test.ts`, and
  `series.test.ts` continue to pass 100%.

---

### Phase 5: CI Zero-Drift Tripwire Check

Verify that the core narrative compiler remains untainted:

```bash
npm run audit:lore

```

- **Pass Condition:** Exit code 0, confirming zero drift on
  `compiled/under-the-floorboards/bible-state.json`.

---

### Output Deliverable Format

Structure your findings in the following format:

1. **Terminal Command Log:**

- Exact commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each capability as **PASSED** or **FAILED**:

- `Harness Boundary Confirmation (apps/995.library untouched)`

- `Compiler Purity Preserved (src/compiler.ts free of NLP imports)`

- `Ontological Verb-Family Taxonomy Configured`

- `Tier 1 Modal Negation & Abstract Object Filters Operational`

- `Tier 1 Fail-Closed Ambiguity Quarantine Operational`

- `Dual-Field AST Reconciliation Verified`

- `Tier 2 Swain MRU Inversion Detector Operational`

- `Tier 2 Sentence Variance, Crutch Density, & Slop Filters Operational`

- `Auteur Telemetry Intent-Differentiated Accounting Operational`

- `Benchmark evalSet.json Allocation Verified (N >= 200)`

- `Wilson Score Precision Lower Bound Verified (>= 0.95)`

- `Wilson Score Recall Lower Bound Verified (>= 0.90)`

- `Full Vitest Test Suite Passed (npm run test:lore)`

- `Zero-Drift Canonical Audit Passed (npm run audit:lore)`

3. **Identified Violations & Immediate Patches:**

- If any check fails, provide the exact file path, line number, and code patch
  required.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Semantic Linter & Lexical Gate Certified)** or
  **BLOCKED (Audit Gate Tripped)**.

```

```
