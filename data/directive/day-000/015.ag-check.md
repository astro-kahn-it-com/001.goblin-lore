# TASK: Antigravity / QA Audit & Verification — `001.lore` Engine & Storyworld Baseline

## REPOSITORY: `camp-candor/000.repo-bot` (or monorepo root)

## AUDIT TARGETS: `packages/001.lore`, `series/under-the-floorboards`, and `apps/995.library`

You are an expert systems architect and autonomous QA verification engineer
auditing the newly provisioned compile-time narrative legislative package
**`001.lore`** (`@camp_candor/001.lore`) and its baseline storyworld
**`series/under-the-floorboards`**[cite: 1, 2, 4].

Your mandate is to use your autonomous terminal execution, file inspection, and
static analysis tools to verify that:

1. The **Immutable Runner Boundary** of `apps/995.library/` was strictly
   preserved with zero modifications[cite: 1].
2. The core compile-time legislative authority lives cleanly in
   `packages/001.lore/` with no runtime or LLM leakage[cite: 2, 4, 30].
3. The foundational Three-Pass Compiler executes with full deterministic
   reproducibility, strict graph cycle checks, and SHA-256 state sealing[cite:
   4, 16, 30].
4. The hand-authored Markdown dossiers in `series/under-the-floorboards/` pass
   the Byte-0 Frontmatter law and Zod schema contracts[cite: 1, 2, 13, 30].
5. The Blessed Curses terminal harness integration (`00.lore.unit`,
   `98.menu.unit`, `BEE.ts`, `hunt.ts`) successfully compiles and wires into the
   monorepo runner without code injection[cite: 1, 5, 29].

Do NOT modify or rewrite functional code without reporting your findings first.
Run direct terminal commands, inspect source files, and provide an objective
PASSED / FAILED verification report with actionable remediation if defects are
encountered.

---

### Phase 1: Boundary & File Tree Inspection

Inspect the filesystem and verify that the exact directory layout exists without
unauthorized packages or framework contamination:

1. **Immutable Boundary Audit (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** The command must return zero lines (completely clean,
     untouched, and uncommitted)[cite: 1].
   - Confirm that no new units, reducers, actions, or menu choices were placed
     inside `apps/995.library/`[cite: 1].

2. **Package Layout & Purity Check (`packages/001.lore`):**
   - Confirm the existence of:
     - `packages/001.lore/package.json` (name: `@camp_candor/001.lore`, private:
       true, type: module)[cite: 1]
     - `packages/001.lore/tsconfig.json` (references monorepo base, composite:
       true, NodeNext)[cite: 1]
     - `packages/001.lore/BEE.ts` and `hunt.ts` (exposes reactive reducer
       registration for terminal dynamic loading)[cite: 1]
     - `packages/001.lore/schemas/` (`agent.schema.ts`, `debt.schema.ts`,
       `spatial.schema.ts`, `unresolved.schema.ts`, `index.ts`)[cite: 1, 2, 4]
     - `packages/001.lore/src/compiler.ts` (Three-pass compiler engine)[cite: 4,
       16, 30]
     - `packages/001.lore/00.lore.unit/` and `packages/001.lore/98.menu.unit/`
       (Standard Blessed action/reducer/buzz units)[cite: 1]
   - Verify that `packages/001.lore/` contains **zero** runtime game engines,
     dice-rolling math, screenplay prompt templates, ComfyUI connectors, or
     external network fetch loops[cite: 2, 4, 30].

3. **Storyworld Dossier Check (`series/under-the-floorboards`):**
   - Confirm the presence of:
     - `series/under-the-floorboards/series.config.json`
     - `series/under-the-floorboards/characters/bog.md`
     - `series/under-the-floorboards/characters/spleen.md`
     - `series/under-the-floorboards/grievances/resource_hoarding.md`
     - `series/under-the-floorboards/locations/the_warm_pipe.md`
     - `series/under-the-floorboards/possessions/silver_spoon.md`

---

### Phase 2: Schema Invariant & Byte-0 Auditing

Inspect the schema definitions and storyworld dossiers for strict architectural
compliance:

1. **Byte-0 Frontmatter Law Verification:**
   - Inspect each authored `.md` file in `series/under-the-floorboards/` using
     hex/byte inspection:
     - Verify that byte 0 of each file is ASCII `---` (`0x2D 0x2D 0x2D 0x0A` or
       `0x2D 0x2D 0x2D 0x0D 0x0A`)[cite: 13, 30].
     - Assert there is zero leading whitespace, blank lines, or UTF-8 BOM
       (`0xEF 0xBB 0xBF`)[cite: 13].

2. **Tri-Partite Vector Inspection (`schemas/agent.schema.ts`):**
   - Verify that `AgentSchema` defines the three isolated vectors[cite: 1]:
     - **Somatic:** `locomotion_baseline` (enum including `bipedal_hunched`,
       `limping`), `banned_kinetic_verbs` (string array), `conditions`,
       `motor_limitations`, `signature_tics`[cite: 1, 2].
     - **Logistical:** `worn`, `held`, `carried`, `cached` (string arrays of
       possession IDs)[cite: 1, 2].
     - **Epistemic:** `escalation_ceiling` (integer 1–5), `leverage_strings`
       (record of string to int), `relationship_defaults`,
       `strings_held_over`[cite: 1, 2].
   - Confirm that all condition severities or scalar envelopes across schemas
     use integer basis points ($0 \le x \le 10{,}000$) or bounded integers (no
     arbitrary floating-point scalars)[cite: 12, 30].

3. **Ignorance by Omission & Mystery Contracts
   (`schemas/unresolved.schema.ts`):**
   - Confirm `UnresolvedMysterySchema` enforces `trigger_lemmas` and
     `ignorance_contract` strings to prevent LLM prompt hallucination[cite: 2,
     4, 14].

---

### Phase 3: Compiler Gauntlet Verification (`src/compiler.ts`)

Audit the implementation of `compileLoreInstance` in
`packages/001.lore/src/compiler.ts` against the three compile passes[cite: 4,
16, 30]:

1. **Pass 1 (Bounds & Frontmatter Isolation):**
   - Verifies `gray-matter` extracts frontmatter starting at byte 0 and executes
     Zod `.parse()`[cite: 1, 4].
   - Throws a fatal, non-zero error immediately on schema violations or scalar
     bound breaches[cite: 1, 4].

2. **Pass 2 (Relational Sweeps & DAG Cycle Detection):**
   - Verifies foreign keys across `characters`, `possessions`, `locations`, and
     `grievances`[cite: 1, 4]:
     - If an item in a character's `worn`, `held`, `carried`, or `cached` slot
       is absent from `possessions`, compilation halts with a diagnostic
       coordinate[cite: 1, 4].
     - If a primary grievance participant is missing from `characters`,
       compilation halts[cite: 2, 4].
   - Verifies Depth-First Search (DFS) traversal on
     `spawns_on_max_escalation`[cite: 4, 6]:
     - Assert that circular grievance escalation chains (e.g., $A \to B \to A$)
       throw a fatal cycle exception[cite: 2, 4, 6].

3. **Pass 3 (Deterministic Sealing & Unicode NFC):**
   - Confirms recursive key sorting (lexicographical via UTF-8 code point)[cite:
     4, 16].
   - Confirms Unicode Normalization Form C (`.normalize('NFC')`) on serialized
     JSON strings[cite: 4].
   - Confirms cryptographic SHA-256 hash generation ($H_{\text{root}}$) written
     atomically into `compiled/bible-state.json` alongside metadata[cite: 3, 4,
     16].
   - Confirms execution is pure: `Date.now()` or ambient clock leaks are
     completely absent from compiler logic[cite: 25].

---

### Phase 4: Terminal Suite & CI Gate Execution

Execute the following verification commands via your terminal harness and report
exit codes and stdout/stderr:

1. **TypeScript Static Check:**
   ```bash
   npm run build --workspace=@camp_candor/001.lore
   ```

````

* Assert zero compiler errors (`tsc -b` exits with code 0).



2. **Package Unit Test Suite:**
```bash
npm run test --workspace=@camp_candor/001.lore

````

- Confirm that `test/compiler.test.ts` executes and passes both clean
  compilation and cyclic DAG failure assertion tests.

3. **Direct Compilation Pass:**

```bash
npm run compile:lore

```

- Confirm `series/under-the-floorboards/compiled/bible-state.json` is generated
  with valid JSON structure, populated character records (`char_bog`,
  `char_spleen`), and a 64-character hex state hash.

4. **Zero-Drift Tripwire:**

```bash
npm run audit:lore

```

- Confirm `git diff --exit-code series/` exits cleanly with code 0.

5. **Blessed Harness Dry-Run Smoke Test:**

- Inspect `apps/995.library/run.ts`'s dynamic loader compatibility with
  `packages/001.lore/BEE.ts`.

- Ensure running the compiler or reading `hunt.ts` exports does not throw
  unhandled promise rejections or import path mismatches.

---

### Output Deliverable Format

Structure your audit response in the following format:

1. **Command Execution Log:**

- Exact commands executed, process exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table evaluating each item as **PASSED** or **FAILED**:
- `Immutable Runner Fence (apps/995.library untouched)`

- `Byte-0 Frontmatter Guarantee (series/ files start with '---')`

- `Tri-Partite Vector Schemas (Somatic, Logistical, Epistemic)`

- `Pass 2 Relational Integrity & DAG Cycle Sweep (DFS)`

- `Pass 3 Deterministic JSON Normalization (NFC + SHA-256)`

- `TypeScript Workspace Compilation (tsc -b)`

- `Vitest Suite Green (compiler.test.ts)`

- `Zero-Drift Tripwire (git diff --exit-code)`

3. **Identified Violations & Immediate Patches:**

- If any check fails (e.g., missing enum member, unhandled import, dangling
  link), provide the exact file path and code patch required to resolve it.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Production Certified at $T_0$)** or **BLOCKED
  (Audit Gate Tripped)**.

```

```
