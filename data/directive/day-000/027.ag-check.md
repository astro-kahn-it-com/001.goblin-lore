# TASK: Antigravity Audit & Verification — Pardon Ledger & Pass 2 Relational Waiver Engine

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## AUDIT TARGETS: `packages/001.lore/schemas`, `packages/001.lore/schemas/ontology`, `packages/001.lore/src/compiler.ts`, `packages/001.lore/test/pardon.test.ts`, and `apps/995.library`

You are an expert systems architect and autonomous QA verification
engineer[cite: 2, 4]. Your mandate is to conduct a forensic audit of the newly
integrated **Pardon Ledger & Relational Waiver Engine** in
`packages/001.lore`[cite: 1, 3, 4].

Under the **Universal Negative-Control Law**, a governance system that has never
been observed rejecting an illegal waiver has not been proven to work[cite: 5,
7, 13]. You must execute terminal commands, inspect source files, verify schema
refinements, evaluate negative control paths, and produce an objective **PASSED
/ FAILED** report[cite: 2, 4].

Do not rewrite working files without reporting your findings first[cite: 2, 4].

---

### Phase 1: Boundary Integrity & Scope Fence Audit

1. **Harness Boundary Check (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** The command returns zero lines (completely clean and
     untouched)[cite: 1, 4].

2. **Core Compiler Non-Proliferation:**
   - Inspect `packages/001.lore/src/compiler.ts`[cite: 4].
   - Confirm that no external HTTP clients, database drivers, or runtime
     dependencies were introduced[cite: 1, 2, 3].
   - Confirm that Pass 1, Pass 2, and Pass 3 execute entirely in-memory using
     pure Node.js built-ins (`node:fs`, `node:path`, `node:crypto`) and
     `gray-matter`/`zod`[cite: 2, 4].

---

### Phase 2: Static Schema & Registry Verification

1. **Structural Self-Defense Refinement
   (`packages/001.lore/schemas/pardon.schema.ts`):**
   - Inspect `RuleSchema`[cite: 1, 4].
   - Verify the `.refine()` predicate enforcing structural consistency:
     - If `severity_class` is `'PHYSICAL_SOMATIC'` or `'EPISTEMIC'`,
       `pardonable` MUST be `false`[cite: 1, 4].
     - Attempting to parse a rule with `severity_class: "PHYSICAL_SOMATIC"` and
       `pardonable: true` must throw a Zod validation error immediately at Pass
       1[cite: 1, 4].
2. **Discriminated State Mutation Payload:**
   - Inspect `InjectedStateMutationSchema`[cite: 2].
   - Confirm that `z.unknown()` and open records are completely eliminated[cite:
     2].
   - Confirm that the schema is a strict `z.discriminatedUnion('vector', [...])`
     across:
     - `somatic`: Validated against `SomaticMutationPayloadSchema`[cite: 2].
     - `logistical`: Validated against `LogisticalMutationPayloadSchema`[cite:
       2].
     - `epistemic`: Validated against `EpistemicMutationPayloadSchema`[cite: 2].
3. **Ontological Registries (`packages/001.lore/schemas/ontology/`):**
   - Confirm `rules.json` exists and parses validly under `RuleSchema`[cite: 1,
     4].
   - Verify that hard somatic rules (`rule_kinetic_limb_conservation`,
     `rule_bimanual_coordination`) declare `pardonable: false`[cite: 4, 11].
   - Verify that hard epistemic rules (`rule_unresolved_epistemic_leak`) declare
     `pardonable: false`[cite: 4, 17].
   - Verify that ontological/stylometric rules (`rule_weight_capacity_ceiling`,
     `rule_mru_causal_inversion`) declare `pardonable: true`[cite: 4, 7].
   - Confirm `authorizers.json` exists and parses validly under
     `AuthorizerSchema`, binding roles to explicit
     `permitted_severity_classes`[cite: 2].
4. **Schema Barrel Export:**
   - Confirm `packages/001.lore/schemas/index.ts` re-exports
     `pardon.schema.js`[cite: 4].

---

### Phase 3: Compiler Pass 1, 2, and 3 Integration Audit

Inspect `packages/001.lore/src/compiler.ts` to confirm the four structural
predicates of `validatePass2Pardons`:

1. **Pass 1 Ingestion:**
   - Verifies that `instanceDir/pardons/*.json` is discovered and parsed
     strictly against `PardonRecordSchema`[cite: 4].
2. **Predicate 1 (Target Entity Foreign-Key Resolution):**
   - Asserts `pardon.target.entity_id` exists in `entities.characters`,
     `locations`, `possessions`, or `grievances`[cite: 4].
   - Fails closed with `[PASS 2 PARDON ERROR]` if the entity is missing[cite:
     4].
3. **Predicate 2 & 3 (Rule Existence & Hard Invariant Fence):**
   - Asserts `pardon.rule_id` resolves in the loaded rule registry[cite: 4].
   - Asserts `rule.pardonable === true`[cite: 4]. If `false`, halts compilation
     unconditionally with `[PASS 2 PARDON FATAL]`[cite: 4].
4. **Predicate 4 (Authorizer Authentication & Role Scope):**
   - Asserts `pardon.authorizer_id` exists in the authorizer registry[cite: 2].
   - Asserts
     `authorizer.permitted_severity_classes.includes(rule.severity_class)`[cite:
     2].
   - Rejects unauthorized waivers with `[PASS 2 PARDON PERMISSION DENIED]`[cite:
     2].
5. **Predicate 5 (Entity-Scoped Stale Hash Isolation):**
   - Verifies that the check calculates the SHA-256 hash of the target entity's
     NFC-normalized JSON[cite: 2, 4].
   - Emits structured diagnostic warning `STALE_ENTITY_PARDON` if
     `granted_against_entity_hash` diverges from current entity state[cite: 2].
   - Confirms that changes to unrelated entities do NOT trigger false-positive
     stale warnings[cite: 2].
6. **Predicate 6 (Rule Waiver Frequency Audit):**
   - Verifies that pardon occurrences per `rule_id` are accumulated[cite: 2, 3].
   - Emits `PARDON_FREQUENCY_EXCEEDED` if usage count exceeds
     `rule.max_tolerated_pardon_frequency`[cite: 2, 3].
   - If `strictPardonAudit: true`, asserts compilation halts closed[cite: 2].
7. **Pass 3 Sealing & Provenance Symmetry:**
   - If active pardons exist, stamps `canonicalArtifact._meta` with
     `provenance_status: "COMPLIANT_BY_EXEMPTION"` and includes
     `applied_pardons`[cite: 3, 4].
   - If zero active pardons exist, ensures the `pardons` key is omitted from
     `entitiesToSeal` so canonical zero-drift parity is preserved[cite: 4].

---

### Phase 4: Automated Vitest Gauntlet & Negative Control Verification

Execute the test runner from the monorepo root:

```bash
# 1. Typecheck the 001.lore package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the dedicated pardon test suite
npx vitest run packages/001.lore/test/pardon.test.ts

# 3. Run the full lore test battery
npm run test:lore

```

**Pass Conditions:**

1. `packages/001.lore/test/pardon.test.ts` passes all 7 discrete negative and
   positive test controls:

- **Refinement Rejection 1:** Rejects `PHYSICAL_SOMATIC` rule declaring
  `pardonable: true`.

- **Refinement Rejection 2:** Rejects `EPISTEMIC` rule declaring
  `pardonable: true`.

- **Pass 2 Fatal Halt:** Rejects waiver targeting unpardonable rule
  `rule_kinetic_limb_conservation`.

- **Pass 2 Dangling Entity:** Rejects waiver targeting non-existent
  `char_ghost`.

- **Pass 2 Permission Denied:** Rejects `auth_scene_director` attempting to
  waive `ONTOLOGY` rule.

- **Pass 2 Provenance Stamping:** Compiles valid waiver on
  `rule_weight_capacity_ceiling` and stamps `COMPLIANT_BY_EXEMPTION`.

- **Pass 2 Stale Entity Detection:** Emits `STALE_ENTITY_PARDON` diagnostic when
  target entity hash diverges.

2. `npm run test:lore` passes all test suites across the package with exit
   code 0.

---

### Phase 5: Canonical Zero-Drift Tripwire Check

Verify that adding the Pardon Ledger subsystem did not mutate or drift the
canonical storyworld snapshot:

```bash
npm run audit:lore

```

- **Pass Condition:** Exit code 0, confirming zero drift on
  `compiled/under-the-floorboards/bible-state.json`.

---

### Output Deliverable Format

Format your audit findings using this structure:

1. **Terminal Command Log:**

- Commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each capability as **PASSED** or **FAILED**:
- `Harness Boundary Confirmation (apps/995.library untouched)`

- `Compiler Purity Maintained (src/compiler.ts dependencies clean)`

- `Structural Schema Refinement Verified (Hard rules cannot be pardonable)`

- `Discriminated State Mutation Payload Enforced (No z.unknown)`

- `Ontological Registries Seeded (rules.json & authorizers.json)`

- `Pass 2 Predicate 1: Target Entity Foreign-Key Resolution`

- `Pass 2 Predicate 2 & 3: Rule Resolution & Hard Invariant Fence`

- `Pass 2 Predicate 4: Authorizer Authentication & Role Scope`

- `Pass 2 Predicate 5: Entity-Scoped Stale Hash Verification`

- `Pass 2 Predicate 6: Waiver Frequency Auditing & Threshold Alert`

- `Pass 3 Sealing & Provenance Status Stamped (COMPLIANT_BY_EXEMPTION)`

- `Pardon Unit Test Suite Passed (packages/001.lore/test/pardon.test.ts)`

- `Complete Lore Battery Passed (npm run test:lore)`

- `Zero-Drift Canonical Audit Passed (npm run audit:lore)`

3. **Identified Violations & Code Patches:**

- If any check fails, state the exact file path, line number, and minimal patch
  required to resolve it.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Pardon Ledger & Relational Waiver Engine
  Certified)** or **BLOCKED (Audit Gate Tripped)**.

```

```
