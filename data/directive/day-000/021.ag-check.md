# TASK: Antigravity Audit & Verification — Comprehensive Referential Integrity & Boundary Confirmation

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## TARGET WORKSPACES: `packages/001.lore`, `series/`, `compiled/`, and `apps/995.library`

You are an expert systems architect and autonomous QA verification
engineer[cite: 5]. Your objective is to audit
`packages/001.lore/src/compiler.ts` and
`packages/001.lore/test/compiler.test.ts` to confirm that the referential
integrity engine (Pass 2) has been fully hardened against all dangling pointer
vectors and that the repository boundaries are preserved[cite: 4].

Do not rewrite or modify working code without reporting your findings
first[cite: 5]. Execute terminal validation commands directly, inspect files,
and provide an objective PASSED / FAILED verification report[cite: 5].

---

### Phase 1: Boundary & Workspace Scope Audit

1. **Harness Boundary Confirmation (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** Confirm that `apps/995.library/995.library/**` has zero
     modifications (clean git status)[cite: 1, 4].
   - Confirm that the **only** permitted modification path within
     `apps/995.library/` is `apps/995.library/run.ts`[cite: 4].
2. **Git Tracking Policy:**
   - Confirm that `.gitignore` does not contain `compiled/**/*_*.json`[cite: 4].
     Both canonical `bible-state.json` and timestamped snapshots must remain
     tracked by Git[cite: 4].

---

### Phase 2: Static Code Inspection of Pass 2 (`packages/001.lore/src/compiler.ts`)

Inspect Pass 2 inside `packages/001.lore/src/compiler.ts` and verify that every
one of the following foreign-key validation checks is actively implemented with
fail-closed errors[cite: 4]:

1. **Character Foreign Keys & Epistemic Social Graph:**
   - **Spatial Pointer:** Asserts `character.location` resolves to an active
     node in `entities.locations`[cite: 4].
   - **Logistical Equip Slots:** Sweeps `worn`, `held`, `carried`, and `cached`
     arrays, asserting every item ID resolves in `entities.possessions`[cite:
     4].
   - **Epistemic Strings Held Over:** Sweeps
     `character.epistemic.strings_held_over`, asserting every target resolves to
     an active character in `entities.characters` and asserting no character
     holds a string over itself (`charId !== debtTarget`)[cite: 4].
   - **Epistemic Leverage Strings:** Sweeps keys of
     `character.epistemic.leverage_strings`, asserting every target character
     exists in `entities.characters` and asserting no character holds leverage
     over itself[cite: 4].
   - **Epistemic Relationship Defaults:** Sweeps keys of
     `character.epistemic.relationship_defaults`, asserting every referenced
     character exists in `entities.characters` and asserting no character maps a
     relationship default to itself[cite: 4].

2. **Spatial Topology Adjacency:**
   - **Adjacency Resolution:** Sweeps `location.adjacent_locations`, asserting
     every neighbor ID resolves in `entities.locations`[cite: 4].
   - **Self-Adjacency Exclusion:** Asserts no location declares adjacency to
     itself (`neighborId !== locId`)[cite: 4].

3. **Grievance Participant Coverage:**
   - **Primary Participants:** Sweeps `grievance.participants_primary`,
     asserting every character ID exists in `entities.characters`[cite: 4].
   - **Secondary Participants:** Sweeps `grievance.participants_can_involve`,
     asserting every character ID exists in `entities.characters`[cite: 4].

4. **Escalation Targets & Causal DAG Acyclicity:**
   - **Target Existence:** Asserts every ID in
     `grievance.spawns_on_max_escalation` resolves in
     `entities.grievances`[cite: 4].
   - **Three-Color / Recursion Stack DFS:** Traverses `spawns_on_max_escalation`
     with a recursion stack, immediately throwing
     `[PASS 2 ERROR] Cyclic grievance escalation detected` if an active ancestor
     node is encountered[cite: 4, 12].

---

### Phase 3: Negative Control & Vitest Gauntlet Verification

Audit `packages/001.lore/test/compiler.test.ts` to verify that the universal
negative-control law is fulfilled with dedicated test fixtures for each error
vector[cite: 4, 20]:

1. **Verify Test Cases in `test/compiler.test.ts`:**
   - Confirm the presence of test assertions proving fatal rejections for:
     - Missing character location (`loc_missing_abyss`)[cite: 4].
     - Missing equipped possession in logistical slots
       (`item_ghost_blade`)[cite: 4].
     - Dangling `strings_held_over` reference (`char_nonexistent`)[cite: 4].
     - Dangling `leverage_strings` key (`char_ghost`)[cite: 4].
     - Dangling `relationship_defaults` key (`char_phantom`)[cite: 4].
     - Dangling `adjacent_locations` neighbor (`loc_portal_to_nowhere`)[cite:
       4].
     - Dangling secondary grievance participant in `participants_can_involve`
       (`char_absent_witness`)[cite: 4].
     - Cyclic grievance spawn escalation ($g_1 \to g_2 \to g_1$)[cite: 4].
     - Canonical baseline compilation passing cleanly for
       `series/under-the-floorboards`[cite: 4].

2. **Execute Terminal Build & Test Gauntlet:** Run the following commands in
   sequence and capture output:
   ```bash
   # 1. Typecheck the 001.lore package
   npm run build --workspace=@camp_candor/001.lore

   # 2. Execute unit tests across the package
   npm run test:lore

   # 3. Verify zero-drift on the canonical storyworld snapshot
   npm run audit:lore
   ```

```

---

### Phase 4: Dynamic Series Cross-Validation Probe

Execute a headless probe verifying that dangling pointer checks remain strictly hermetic across dynamic series:

1. Dispatch `ActSer.CREATE_SERIES` with `src: "hermetic-probe"`.


2. Confirm the series scaffolds and immediately pre-compiles cleanly at $T_0$.


3. Intentionally inject an invalid cross-series reference: add `item_silver_spoon` (which only exists in `under-the-floorboards`) into `series/hermetic-probe/characters/protagonist.md` under `logistical.held`.


4. Run `compileLoreInstance` on `series/hermetic-probe` and confirm it halts with `[PASS 2 ERROR] Character 'char_hermetic-probe_protagonist' equips missing possession in 'held': 'item_silver_spoon'`.


5. Teardown: remove `series/hermetic-probe/` and `compiled/hermetic-probe/`.



---

### Output Deliverable Format

Output your audit findings in the following format:

1. **Terminal Command Log:**
* Executed commands, exit codes, and stdout/stderr summaries.




2. **Acceptance Criteria Verification Matrix:**
* A Markdown table grading each check as **PASSED** or **FAILED**:
* `Harness Boundary Confirmation (apps/995.library/995.library/** untouched)`

* `Sole Modification Exception Verified (apps/995.library/run.ts only)`

* `Character Location Pointer Validation`

* `Character Logistical Equip Slot Pointers (worn, held, carried, cached)`

* `Epistemic Debt Pointers & Self-Debt Rejection (strings_held_over)`

* `Epistemic Leverage Pointers & Self-Leverage Rejection (leverage_strings)`

* `Epistemic Relationship Pointers & Self-Reference Rejection (relationship_defaults)`

* `Spatial Topological Adjacency & Self-Adjacency Rejection (adjacent_locations)`

* `Grievance Primary & Secondary Participant Validation (participants_can_involve)`

* `Grievance Escalation Target Existence & DFS Acyclicity (spawns_on_max_escalation)`

* `Vitest Gauntlet Passed (All 9+ Negative Control Assertions Pass)`

* `Zero-Drift Canonical Audit Passed (npm run audit:lore)`





3. **Identified Violations & Immediate Patches:**
* If any check fails, state the exact file path, line number, and provide the exact patch.




4. **Final Architectural Verdict:**
* Explicitly declare: **PASSED (Referential Integrity & Boundary Law Certified)** or **BLOCKED (Audit Gate Tripped)**.





```
