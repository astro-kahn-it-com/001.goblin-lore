# TASK: Antigravity Audit & Verification — Top-Level Dual-Emission Lore Architecture

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## TARGET WORKSPACES: `packages/001.lore`, `compiled/`, `series/under-the-floorboards`, and `apps/995.library`

You are an expert systems architect and autonomous QA verification
engineer[cite: 4, 16]. Your task is to audit the relocation of the compiled
canon artifacts to the top-level directory and verify the implementation of the
**Dual-Emission Snapshot Protocol** (canonical `bible-state.json` + immutable
timestamped `bible-state_<TIMESTAMP>.json`)[cite: 3, 7].

Use your autonomous terminal execution, file inspection, and static analysis
capabilities to audit the changes. Do not modify or rewrite functional code
without reporting your findings first[cite: 4, 16]. Run direct terminal
commands, inspect source files, and deliver an objective **PASSED / FAILED**
verification report[cite: 4, 16].

---

### Phase 1: Structural Layout & Boundary Inspection

Inspect the filesystem to ensure clean separation between authored source
markdown and compiled snapshots:

1. **Top-Level `compiled/` Decoupling:**
   - Verify that the compiled artifact output directory lives at
     `<rootDir>/compiled/under-the-floorboards/`[cite: 2, 3].
   - Confirm that the legacy nested folder
     `series/under-the-floorboards/compiled/` has been **completely
     purged/deleted**[cite: 2].
   - Assert that `series/under-the-floorboards/` contains **strictly authored
     Markdown files** (`characters/`, `grievances/`, `locations/`,
     `possessions/`) and `series.config.json`[cite: 2, 3].

2. **Immutable Runner Fence Check (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** The command must return zero lines (completely clean,
     untouched, and uncommitted)[cite: 1, 5].

3. **Git Hygiene Verification (Option 1 Compliance):**
   - Inspect `.gitignore` and confirm that timestamped snapshots
     (`bible-state_*.json`) are **NOT ignored**.
   - Run:
     ```bash
     git check-ignore compiled/under-the-floorboards/bible-state_*.json
     ```
   - **Pass Condition:** The check-ignore command must return empty (exit code
     1), confirming that both canonical HEAD and historical snapshots are
     tracked by Git[cite: 3, 8].

---

### Phase 2: Dual-Emission Artifact & Timestamp Inspection

Inspect the output files generated inside
`<rootDir>/compiled/under-the-floorboards/`:

1. **Dual-Emission File Existence:**
   - Confirm the simultaneous presence of:
     - `compiled/under-the-floorboards/bible-state.json`[cite: 2, 3]
     - At least one timestamped snapshot matching:
       `compiled/under-the-floorboards/bible-state_*.json`[cite: 3]

2. **Sanitized Windows-Safe ISO 8601 Timestamp Formatting:**
   - Verify the timestamped filename pattern:
     ```text
     bible-state_YYYY-MM-DDTHH-mm-ssZ.json (or similar millisecond variant with dashes)
     ```
   - Confirm that colons (`:`) are strictly absent from the filename to ensure
     cross-platform Windows filesystem safety[cite: 3].

3. **Cryptographic Identity & Payload Equivalence:**
   - Parse both `bible-state.json` and the corresponding
     `bible-state_<TIMESTAMP>.json`.
   - Assert that `_meta.state_hash` matches identically across both files[cite:
     4, 16].
   - Perform a content comparison (ignoring the filename itself) to verify that
     both files contain byte-for-byte identical canonical JSON (Unicode NFC
     normalized)[cite: 4, 16].

4. **Zero Ambient Time Leak in Hashed State:**
   - Confirm that the calculated SHA-256 state hash ($H_{\text{root}}$) did not
     incorporate `new Date()` or ambient clock values into the sorted entity
     payload[cite: 4].
   - The timestamp must only exist in the physical archive filename and runtime
     logging metadata[cite: 3, 4].

---

### Phase 3: Compiler Gauntlet & Negative Control Verification

Execute targeted compiler audits and failure modes:

1. **Compiler Execution:**
   - Run:
     ```bash
     npm run compile:lore
     ```
   - Verify that compilation executes without errors, logs both the emitted HEAD
     path and timestamped snapshot path to the console, and exits with code 0.

2. **Negative Control Verification (The Universal Negative-Control Law):**
   - Test whether the compiler reliably trips on a deliberate defect[cite: 8]:
     ```bash
     # Temporarily inject a cyclic dependency into a fixture or test instance
     npm run test:lore
     ```
   - Confirm that `packages/001.lore/test/compiler.test.ts` executes and passes
     its cyclic grievance assertion (proving that $A \to B \to A$ is rejected
     with a fatal exit)[cite: 2, 4, 6].

---

### Phase 4: CI Zero-Drift Tripwire Audit

Validate that root `package.json` correctly guards against drift:

1. **Script Target Inspection:**
   - Inspect root `package.json` under `"scripts"`.
   - Confirm the `audit:lore` script targets the new top-level path:
     ```json
     "audit:lore": "npm run compile:lore && git diff --exit-code compiled/under-the-floorboards/bible-state.json"
     ```

2. **Tripwire Execution:**
   - Run:
     ```bash
     npm run audit:lore
     ```
   - Confirm the exit code is 0, certifying zero drift between authored source
     Markdown and the committed canonical snapshot[cite: 4, 16, 22].

---

### Output Deliverable Format

Structure your verification findings in the following format:

1. **Terminal Command Log:**
   - Commands executed, exit codes, and stdout/stderr summaries.
2. **Acceptance Criteria Verification Matrix:**
   - A Markdown table grading each requirement as **PASSED** or **FAILED**:
     - `Immutable Harness Fence (apps/995.library untouched)`[cite: 1, 5]
     - `Top-Level compiled/ Directory Structure`[cite: 2, 3]
     - `Legacy series/.../compiled/ Purged`[cite: 2]
     - `Dual-Emission Emitted (bible-state.json + bible-state_<TIMESTAMP>.json)`[cite:
       3, 7]
     - `Windows-Safe Sanitized Filename (No colons in timestamp)`[cite: 3]
     - `Payload Identity & Hash Equivalence ($H_{\text{root}}$ matches across both)`[cite:
       4, 16]
     - `Git Tracking Option 1 (Timestamped snapshots NOT in .gitignore)`[cite:
       3, 8]
     - `Zero-Drift Tripwire Pass (npm run audit:lore)`[cite: 4, 16, 22]
3. **Identified Violations & Immediate Patches:**
   - If any check fails, state the exact file path and code patch required to
     resolve it[cite: 3, 4].
4. **Final Architectural Verdict:**
   - Explicitly declare: **PASSED (Top-Level Dual-Emission Certified)** or
     **BLOCKED (Audit Gate Tripped)**[cite: 4, 8].
