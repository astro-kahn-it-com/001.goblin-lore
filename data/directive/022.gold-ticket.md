# TASK SPECIFICATION FOR JULES CODING AGENT

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## TARGET WORKSPACE: `packages/001.lore`

## FILES TO CREATE / OVERWRITE:

## - `packages/001.lore/test/fixtures/failures/**` (Golden Failure Fixture Trees)

## - `packages/001.lore/test/golden_failures.test.ts` (Golden Meta-Runner Gauntlet)

## EPIC: `EPIC-04-GOLDEN-FILE-FAILURE-GAUNTLET`

---

### I. SYSTEM LAWS & IMMUTABILITY BOUNDARIES (ATTENTION TOP ANCHOR)

You are an automated, sandboxed execution worker operating within strict
monorepo guardrails[cite: 1, 3]:

- **IMMUTABLE RUNNER BOUNDARY:** You are strictly forbidden from modifying any
  file inside `apps/995.library/`[cite: 1, 4].
- **WORKSPACE SCOPE:** You may ONLY create and modify files inside
  `packages/001.lore/test/`[cite: 1, 3].
- **ZERO-LEAK & ZERO-EMISSION INVARIANT:** On a failed compilation pass, the
  compiler must halt closed without writing partial `.json` files or creating
  directory residue inside `compiled/`[cite: 3, 4].
- **THE UNIVERSAL NEGATIVE-CONTROL LAW:** Every failure vector (Pass 1 byte-0
  violations, scalar overflows, float basis points, Pass 2 foreign key breaks,
  self-debt anomalies, and DAG cycles) must be codified into static, committed
  disk fixtures[cite: 4, 10]. Programmatic dynamic strings are prohibited for
  golden regression testing.
- **WINDOWS CMD & POSIX ENCODING SAFETY:** Ensure test assertions and file
  encodings work seamlessly across Windows and POSIX[cite: 3, 4]. Do not use
  multi-byte unicode emojis in test logs[cite: 3, 7].
- **FAIL-SAFE EXIT:** If any assertion fails or typechecks break, immediately
  halt and emit: `CONFLICT_BLOCKED`[cite: 2, 3].

---

### II. ARCHITECTURAL MISSION & REQUIREMENTS

Complete **Step 3 (Build the Golden-File Invariant & Failure Test Suite)** by
creating static, committed failure fixtures on disk and a dedicated Golden
Meta-Runner[cite: 4]:

1. **Static Golden Failure Fixture Trees
   (`packages/001.lore/test/fixtures/failures/`):** Scaffold permanently
   committed fixture suites representing isolated negative controls. Each
   fixture directory must contain the minimal set of Markdown dossiers needed to
   provoke the failure, accompanied by an `expected_error.json` defining the
   failure pass, error code, and regex pattern[cite: 4]:
   - `pass1_err_byte0_bom`: Dossier prepended with UTF-8 BOM (`\uFEFF---`).
   - `pass1_err_byte0_whitespace`: Dossier prepended with leading newline/space
     (`\n---`).
   - `pass1_err_scalar_overflow`: Grievance with intensity envelope $> 10{,}000$
     basis points.
   - `pass1_err_float_basis_points`: Grievance with floating-point scalars (e.g.
     `[100.5, 5000.25]`).
   - `pass1_err_escalation_ceiling`: Character with `escalation_ceiling` $> 5$.
   - `pass2_err_dangling_location`: Character referencing non-existent location.
   - `pass2_err_dangling_possession`: Character equipping non-existent item.
   - `pass2_err_self_leverage`: Character declaring leverage over itself.
   - `pass2_err_cyclic_grievance`: Circular grievance escalation chain
     ($g_1 \to g_2 \to g_1$).

2. **The Golden Meta-Runner
   (`packages/001.lore/test/golden_failures.test.ts`):**
   - Dynamically iterates over all folders in `test/fixtures/failures/`.
   - Executes `compileLoreInstance` against each fixture directory.
   - Asserts that compilation throws an error matching `expected_error.json`.
   - Asserts the **Deletion Principle & Zero-Emission Invariant**: verifies that
     the target `compiled/` directory was never created on disk during failed
     runs[cite: 3, 4].

---

### III. CONCRETE FILE IMPLEMENTATIONS

#### 1. Scaffold Failure Fixtures (`packages/001.lore/test/fixtures/failures/`)

##### A. `pass1_err_byte0_bom`

- `packages/001.lore/test/fixtures/failures/pass1_err_byte0_bom/characters/bad_bom.md`

```markdown
---

 id: char_bom name: Bad BOM type: character somatic: locomotion_baseline:
bipedal_standard logistical: {} epistemic: {
--
```

_(Note: Byte 0 starts with UTF-8 BOM `0xEF 0xBB 0xBF` / `\uFEFF` before `---`)_

- `packages/001.lore/test/fixtures/failures/pass1_err_byte0_bom/expected_error.json`

```json
{
  "pass": 1,
  "error_code": "BYTE_ZERO_VIOLATION",
  "expected_message_regex": "\\[PASS 1 ERROR\\] File bad_bom\\.md does not begin with YAML frontmatter at byte 0\\."
}
```

---

##### B. `pass1_err_byte0_whitespace`

- `packages/001.lore/test/fixtures/failures/pass1_err_byte0_whitespace/characters/leading_space.md`

```markdown
---

id: char_space name: Leading Space type: character somatic: locomotion_baseline:
bipedal_standard logistical: {} epistemic: {}
---
```

_(Note: Byte 0 starts with a newline `\n` before `---`)_

- `packages/001.lore/test/fixtures/failures/pass1_err_byte0_whitespace/expected_error.json`

```json
{
  "pass": 1,
  "error_code": "BYTE_ZERO_VIOLATION",
  "expected_message_regex": "\\[PASS 1 ERROR\\] File leading_space\\.md does not begin with YAML frontmatter at byte 0\\."
}
```

---

##### C. `pass1_err_scalar_overflow`

- `packages/001.lore/test/fixtures/failures/pass1_err_scalar_overflow/grievances/overflow.md`

```markdown
---
id: grv_overflow
type: grievance
participants_primary: ['char_placeholder']
intensity_envelope: [0, 10001]
spawns_on_max_escalation: []
resolved: false
---
```

- `packages/001.lore/test/fixtures/failures/pass1_err_scalar_overflow/expected_error.json`

```json
{
  "pass": 1,
  "error_code": "SCALAR_OVERFLOW",
  "expected_message_regex": "Number must be less than or equal to 10000"
}
```

---

##### D. `pass1_err_float_basis_points`

- `packages/001.lore/test/fixtures/failures/pass1_err_float_basis_points/grievances/float.md`

```markdown
---
id: grv_float
type: grievance
participants_primary: ['char_placeholder']
intensity_envelope: [100.5, 5000.25]
spawns_on_max_escalation: []
resolved: false
---
```

- `packages/001.lore/test/fixtures/failures/pass1_err_float_basis_points/expected_error.json`

```json
{
  "pass": 1,
  "error_code": "FLOAT_DISALLOWED",
  "expected_message_regex": "Expected integer, received float"
}
```

---

##### E. `pass1_err_escalation_ceiling`

- `packages/001.lore/test/fixtures/failures/pass1_err_escalation_ceiling/characters/ceiling_overflow.md`

```markdown
---
id: char_ceiling
name: Ceiling Overflow
type: character
somatic:
  locomotion_baseline: bipedal_standard
logistical: {}
epistemic:
  escalation_ceiling: 6
---
```

- `packages/001.lore/test/fixtures/failures/pass1_err_escalation_ceiling/expected_error.json`

```json
{
  "pass": 1,
  "error_code": "CEILING_BOUND_EXCEEDED",
  "expected_message_regex": "Number must be less than or equal to 5"
}
```

---

##### F. `pass2_err_dangling_location`

- `packages/001.lore/test/fixtures/failures/pass2_err_dangling_location/characters/lost_character.md`

```markdown
---
id: char_lost
name: Lost
type: character
location: loc_abyss
somatic:
  locomotion_baseline: bipedal_standard
logistical: {}
epistemic: {}
---
```

- `packages/001.lore/test/fixtures/failures/pass2_err_dangling_location/expected_error.json`

```json
{
  "pass": 2,
  "error_code": "DANGLING_LOCATION",
  "expected_message_regex": "\\[PASS 2 ERROR\\] Character 'char_lost' references missing location: 'loc_abyss'"
}
```

---

##### G. `pass2_err_dangling_possession`

- `packages/001.lore/test/fixtures/failures/pass2_err_dangling_possession/characters/equipper.md`

```markdown
---
id: char_equipper
name: Equipper
type: character
somatic:
  locomotion_baseline: bipedal_standard
logistical:
  held: ['item_phantom_blade']
epistemic: {}
---
```

- `packages/001.lore/test/fixtures/failures/pass2_err_dangling_possession/expected_error.json`

```json
{
  "pass": 2,
  "error_code": "DANGLING_POSSESSION",
  "expected_message_regex": "\\[PASS 2 ERROR\\] Character 'char_equipper' equips missing possession in 'held': 'item_phantom_blade'"
}
```

---

##### H. `pass2_err_self_leverage`

- `packages/001.lore/test/fixtures/failures/pass2_err_self_leverage/characters/narcissist.md`

```markdown
---
id: char_narcissist
name: Narcissist
type: character
somatic:
  locomotion_baseline: bipedal_standard
logistical: {}
epistemic:
  leverage_strings: { 'char_narcissist': 3 }
---
```

- `packages/001.lore/test/fixtures/failures/pass2_err_self_leverage/expected_error.json`

```json
{
  "pass": 2,
  "error_code": "SELF_LEVERAGE_DISALLOWED",
  "expected_message_regex": "\\[PASS 2 ERROR\\] Character 'char_narcissist' cannot hold leverage over itself\\."
}
```

---

##### I. `pass2_err_cyclic_grievance`

- `packages/001.lore/test/fixtures/failures/pass2_err_cyclic_grievance/characters/char_anchor.md`

```markdown
---
id: char_anchor
name: Anchor
type: character
somatic:
  locomotion_baseline: bipedal_standard
logistical: {}
epistemic: {}
---
```

- `packages/001.lore/test/fixtures/failures/pass2_err_cyclic_grievance/grievances/g1.md`

```markdown
---
id: grv_g1
type: grievance
participants_primary: ['char_anchor']
intensity_envelope: [0, 1000]
spawns_on_max_escalation: ['grv_g2']
---
```

- `packages/001.lore/test/fixtures/failures/pass2_err_cyclic_grievance/grievances/g2.md`

```markdown
---
id: grv_g2
type: grievance
participants_primary: ['char_anchor']
intensity_envelope: [0, 1000]
spawns_on_max_escalation: ['grv_g1']
---
```

- `packages/001.lore/test/fixtures/failures/pass2_err_cyclic_grievance/expected_error.json`

```json
{
  "pass": 2,
  "error_code": "CYCLIC_DAG_ESCALATION",
  "expected_message_regex": "\\[PASS 2 ERROR\\] Cyclic grievance escalation detected: 'grv_g1' -> 'grv_g2'"
}
```

---

#### 2. Create Golden Meta-Runner: `packages/001.lore/test/golden_failures.test.ts`

```typescript
import { describe, it, expect } from 'vitest'
import path from 'node:path'
import fs from 'node:fs'
import { compileLoreInstance } from '../src/compiler.js'

describe('Golden-File Negative Control Gauntlet (Pass 1 & Pass 2)', () => {
  const failureFixturesDir = path.resolve(
    process.cwd(),
    'test/fixtures/failures',
  )
  const failureCases = fs
    .readdirSync(failureFixturesDir, { withFileTypes: true })
    .filter((d) => d.isDirectory())
    .map((d) => d.name)

  for (const testCase of failureCases) {
    it(`fails closed on static fixture: ${testCase}`, () => {
      const caseDir = path.join(failureFixturesDir, testCase)
      const expectedErrorFile = path.join(caseDir, 'expected_error.json')

      expect(fs.existsSync(expectedErrorFile)).toBe(true)
      const expected = JSON.parse(fs.readFileSync(expectedErrorFile, 'utf-8'))

      const dummyCompiledRoot = path.join(caseDir, 'compiled')

      // Assert compilation halts closed with the exact expected error message
      expect(() =>
        compileLoreInstance({
          instanceDir: caseDir,
          compiledRootDir: dummyCompiledRoot,
          seriesSlug: testCase,
        }),
      ).toThrow(new RegExp(expected.expected_message_regex))

      // Assert Zero-Emission Invariant / Deletion Principle:
      // A failed compilation pass MUST NEVER write partial state or output directories to disk
      expect(fs.existsSync(dummyCompiledRoot)).toBe(false)
    })
  }
})
```

---

### IV. FALSIFIABLE ACCEPTANCE CRITERIA & VERIFICATION (BOTTOM ANCHOR)

Execute this exact command sequence in your terminal before completing the task:

```bash
# 1. Typecheck the 001.lore package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the complete test suite including golden failure fixtures
npm run test:lore

# 3. Assert zero changes to apps/995.library/
git status -s apps/995.library/

# 4. Verify canonical series under-the-floorboards passes audit cleanly
npm run audit:lore

```

**MANDATORY PASS REQUIREMENTS:**

- `npm run build --workspace=@camp_candor/001.lore` exits with code 0 (`tsc -b`
  passes).

- `npm run test:lore` executes `test/golden_failures.test.ts` and passes all
  static failure cases without emitting residue.

- `git status -s apps/995.library/` returns 0 modified files (completely
  untouched).

- `npm run audit:lore` exits with code 0.

```

```
