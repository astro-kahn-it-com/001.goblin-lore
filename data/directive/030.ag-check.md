# TASK: Antigravity Audit & Verification — Epistemic Unresolved Canon Registry & Protection Engine

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## AUDIT TARGETS:

## - `packages/001.lore/schemas/unresolved.schema.ts`

## - `packages/001.lore/schemas/ontology/unresolved.json`

## - `packages/001.lore/src/linters/epistemicLinter.ts`

## - `packages/001.lore/src/prompt/contextAssembly.ts`

## - `packages/001.lore/src/compiler.ts`

## - `packages/001.lore/test/epistemic.test.ts`

## - `apps/995.library/` (Immutable Boundary Check)

You are an expert systems architect and autonomous QA verification
engineer[cite: 1]. Your mandate is to conduct a forensic audit of the newly
integrated **Epistemic Unresolved Canon Registry & Protection Engine** in
`packages/001.lore`[cite: 1, 2].

Under the **Universal Negative-Control Law**, an epistemic boundary that has
never been observed rejecting an ungrounded leak or unindexed character
reference has not been proven to work[cite: 1, 11]. Use autonomous terminal
execution, file inspection, and static analysis tools to verify that narrative
ambiguity is preserved, model closure bias is defended against via _Ignorance by
Omission_, and compiler determinism remains uncompromised[cite: 1, 11].

Do not modify functional code without reporting your findings first[cite: 1].
Execute terminal validation commands directly, inspect files, and provide an
objective PASSED / FAILED verification report[cite: 1].

---

### Phase 1: Boundary Integrity & Deterministic Purity Audit

1. **Harness Boundary Check (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** Returns zero lines[cite: 1]. Confirm that the upstream
     harness was not touched[cite: 1].

2. **Deterministic Ambient Time Purity:**
   - Inspect `packages/001.lore/schemas/unresolved.schema.ts`[cite: 1].
   - Confirm that `last_reconsidered_at` defaults strictly to a static integer
     epoch (`0`) rather than evaluating `Date.now()`[cite: 1].
   - Confirm zero imports of dynamic clock or environment APIs within the
     schema[cite: 1].

3. **Compiler Dependency Purity:**
   - Inspect `packages/001.lore/src/compiler.ts`[cite: 1].
   - Confirm that all imports are confined to Node.js built-ins (`node:fs`,
     `node:path`, `node:crypto`, `node:url`), `gray-matter`, and internal
     schemas/linters[cite: 1]. Zero network libraries, third-party parsers, or
     async database connectors are permitted[cite: 1, 2].

---

### Phase 2: Static Schema & Refinement Audit

1. **Epistemic Taxonomy Verification (`schemas/unresolved.schema.ts`):**
   - Confirm that `WorldEpistemicStatusEnum` defines all four canonical world
     states: `'KNOWN_TRUE'`, `'KNOWN_FALSE'`, `'DISPUTED'`, and
     `'UNRESOLVED_BY_CANON'`[cite: 1].
   - Confirm that `EpistemicHorizonEnum` defines all five character horizons:
     `'TOTAL_IGNORANCE'`, `'RUMOR_ONLY'`, `'FALSE_BELIEF'`, `'PARTIAL_FACT'`,
     and `'WITHHELD'`[cite: 1].
   - Confirm that `TriStateStandingEnum` defines `'ACTIVE_ENDORSED'`,
     `'FLAGGED_FOR_REVIEW'`, and `'DEPRECATED_ARCHIVED'`[cite: 4].

2. **Injection Defense & Prompt Boundary Sanitization:**
   - Inspect `DistortedBeliefStringSchema`:
     - Asserts `min(5)` and `max(500)` character bounds to avoid context window
       saturation[cite: 1].
     - Verifies a `.refine()` rule that rejects strings containing square
       brackets (`[` or `]`), preventing prompt delimiter collisions[cite: 1].

3. **Cross-Field Refinement Enforcement:**
   - Verify Refinement 1: Any character assigned `RUMOR_ONLY` or `FALSE_BELIEF`
     in `epistemic_horizons` must have a non-empty string in
     `distorted_beliefs`[cite: 1].
   - Verify Refinement 2: `permitted_clue_tokens` and the flattened members of
     `trigger_lemma_sets` must be mutually disjoint
     ($A \cap B = \emptyset$)[cite: 1].

4. **Ontological Registry Seed (`schemas/ontology/unresolved.json`):**
   - Verify `unresolved.json` exists and parses cleanly under
     `UnresolvedCanonSchema`[cite: 1].
   - Confirm default seed includes `mystery_the_famine` with valid horizons,
     disjoint clue tokens, and a non-empty `human_promotion_condition`[cite: 1].

---

### Phase 3: Logical Epistemic Unit (LEU) & Sliding-Window Linter Audit

Inspect `packages/001.lore/src/linters/epistemicLinter.ts`:

1. **LEU Normalization (`normalizeToLEUs`):**
   - Verify that soft line wraps (`\n`) within an unbroken narrative paragraph
     are collapsed into a continuous single space[cite: 1].
   - Verify that boundaries reset cleanly on:
     - Markdown headings (`#`, `##`, `###`)[cite: 1]
     - Thematic breaks (`---`, `***`)[cite: 1]
     - True paragraph breaks (`\n\s*\n`)[cite: 1]
     - Speaker turns (`[char_id]:`)[cite: 1]
   - Verify that `speakerId` is extracted and attached to the unit when a turn
     tag is present[cite: 1].

2. **Mathematical Window Loop Bound (`scanProse`):**
   - Inspect the sliding-window iterator:
     ```typescript
     const maxIterations = Math.max(0, tokens.length - windowSize)
     for (let i = 0; i <= maxIterations; i++) {
       const windowSlice = tokens.slice(
         i,
         Math.min(i + windowSize, tokens.length),
       )
       // ...
     }
     ```
   - Confirm that for inputs where `tokens.length < windowSize`, the loop
     evaluates exactly once ($i=0$) across all available tokens[cite: 1].
   - Confirm that for inputs where `tokens.length >= windowSize`, the loop
     terminates when the last full window of size `windowSize` (20) has been
     formed[cite: 1].
   - Verify that `break` terminates sliding for the specific `lemmaSet` within
     the unit without aborting evaluations for other lemma sets or
     mysteries[cite: 1].

3. **Presentation Case Preservation:**
   - Confirm `matchedLemmas` retains canonical case from the schema definition,
     and `excerpt` preserves original casing and line formatting from the source
     text[cite: 1].

---

### Phase 4: Context Assembly & Prompt Sanitization Audit

Inspect `packages/001.lore/src/prompt/contextAssembly.ts`:

1. **Ignorance by Omission Enforcement:**
   - Confirm that `buildCharacterEpistemicPrompt` adds the topic ID to
     `omittedTopicIds` and injects zero prompt text when a character is assigned
     `TOTAL_IGNORANCE`[cite: 1, 11].
   - Confirm that unlisted characters default safely to `TOTAL_IGNORANCE`[cite:
     1].

2. **Authored Belief Injection:**
   - Confirm that `RUMOR_ONLY` injects `[UNVERIFIED HEARSAY]` with the authored
     text from `distorted_beliefs`[cite: 1].
   - Confirm that `FALSE_BELIEF` injects `[SUBJECTIVE CONVICTION]` with the
     authored text from `distorted_beliefs`[cite: 1].
   - Confirm that `PARTIAL_FACT` injects `[PERCEPTUAL EVIDENCE]` formatting
     `permitted_clue_tokens`[cite: 1].
   - Confirm that `WITHHELD` injects `[RESTRAINED KNOWLEDGE]` without revealing
     the secret[cite: 1].

3. **Input Sanitization:**
   - Confirm that `sanitizePromptText` strips square brackets (`[` and `]`),
     quotes, and backslashes before string interpolation[cite: 1].

---

### Phase 5: Compiler Integration & Frontmatter Isolation Audit

Inspect `packages/001.lore/src/compiler.ts`:

1. **Cross-Platform Ontology Resolution:**
   - Confirm that `loadOntologyUnresolved` uses `fs.existsSync` to fall back
     between `packages/001.lore/schemas/ontology/unresolved.json` and
     `schemas/ontology/unresolved.json`[cite: 1].

2. **Pass 2 Relational Hard Gate:**
   - In `validatePass2Epistemic`, verify that every character ID mapped in
     `epistemic_horizons` and `distorted_beliefs` must exist in
     `entities.characters`[cite: 1].
   - Asserts that missing character keys halt compilation unconditionally with
     `[PASS 2 EPISTEMIC FATAL]`[cite: 1].

3. **Frontmatter Isolation in Advisory Dossier Scanning:**
   - Confirm that the compiler extracts clean Markdown prose post-frontmatter
     stripping (`matter(raw).content`) before invoking
     `EpistemicLinter.scanProse`[cite: 1].
   - Verify that frontmatter YAML keys and arrays do not trigger false-positive
     co-occurrences[cite: 1].
   - Confirm that findings from dossier prose are returned in
     `epistemicDiagnostics` as advisory alerts without crashing
     `compileLoreInstance`[cite: 1].

4. **Pass 3 Sealing & Provenance:**
   - Confirm that `_meta.epistemic_manifest` is stamped with active mysteries,
     world status, and character horizons when compiling new series[cite: 1].
   - Confirm that the canonical snapshot (`series/under-the-floorboards`)
     maintains byte-for-byte zero drift[cite: 1].

---

### Phase 6: Automated Test Execution & Tripwire Verification

Run the full testing gauntlet from the monorepo root:

```bash
# 1. Typecheck the 001.lore package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the dedicated epistemic test suite
npm run test --workspace=@camp_candor/001.lore -- test/epistemic.test.ts

# 3. Run all unit, golden, linter, pardon, and epistemic test suites
npm run test:lore

# 4. Execute the canonical zero-drift tripwire
npm run audit:lore

# 5. Confirm immutable runner boundary remains pristine
git status -s apps/995.library/

```

**Pass Conditions:**

- `npm run build --workspace=@camp_candor/001.lore` exits with code 0 (`tsc -b`
  passes).

- `test/epistemic.test.ts` passes all 7 unit test assertions:

- Rejects prompt injection brackets in `distorted_beliefs`.

- Rejects clue token and trigger lemma set intersections.

- Detects forbidden co-occurrence across soft-wrapped lines via LEU.

- Resets sliding window across speaker dialogue turns.

- Halts with `[PASS 2 EPISTEMIC FATAL]` on dangling character foreign keys.

- Records advisory dossier diagnostic without interrupting compilation.

- Enforces _Ignorance by Omission_ in context assembly.

- `npm run test:lore` passes 8/8 test files.

- `npm run audit:lore` exits with code 0 with zero drift on `bible-state.json`.

- `git status -s apps/995.library/` returns 0 modified files.

---

### Output Deliverable Format

Structure your findings in the following format:

1. **Terminal Command Log:**

- Commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each capability as **PASSED** or **FAILED**:

- `Immutable Runner Boundary Confirmation (apps/995.library untouched)`

- `Deterministic Ambient Time Purity (last_reconsidered_at: 0)`

- `Compiler Dependency Purity Maintained`

- `Epistemic Taxonomy Enums Defined (World, Horizon, Standing)`

- `Prompt Injection Defense Verified (Bracket rejection & max 500 chars)`

- `Cross-Field Schema Refinements Active (Distorted beliefs & Disjointness)`

- `LEU Normalization & Boundary Resets Verified (Headings, Paragraphs, Turns)`

- `Sliding Window Loop Bounds Sized Correctly (W = 20)`

- `Ignorance by Omission Verified in Context Assembly`

- `Pass 2 Hard Foreign-Key Halts Operational`

- `Pass 2 Clean Post-Frontmatter Dossier Scanning Operational`

- `Dedicated Epistemic Unit Suite Passed (test/epistemic.test.ts)`

- `Full Lore Test Battery Passed (npm run test:lore)`

- `Zero-Drift Canonical Audit Passed (npm run audit:lore)`

3. **Identified Violations & Immediate Patches:**

- If any check fails, state the exact file path, line number, and minimal patch
  required to resolve it.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Epistemic Unresolved Canon Registry Certified)**
  or **BLOCKED (Audit Gate Tripped)**.

```

```
