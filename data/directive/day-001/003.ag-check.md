# TASK: Antigravity Audit & Verification — Interactive Entity Authoring Wizard & Scaffolder Engine (Track C)

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## TARGET WORKSPACE: `packages/001.lore`

## AUDIT TARGETS:

## - `packages/001.lore/src/scaffolder/satSolver.ts`

## - `packages/001.lore/src/scaffolder/reciprocalLinker.ts`

## - `packages/001.lore/src/scaffolder/entityBuilder.ts`

## - `packages/001.lore/src/scaffolder/index.ts`

## - `packages/001.lore/src/index.ts`

## - `packages/001.lore/00.lore.unit/`

## - `packages/001.lore/98.menu.unit/buz/00.menu.buzz.ts`

## - `packages/001.lore/test/scaffold.test.ts`

## - `apps/995.library/` (Immutable Boundary Check)

You are an expert systems architect and autonomous QA verification engineer.
Your mandate is to conduct an audit of the newly integrated **Interactive Entity
Authoring Wizard in Blessed TUI (EPIC-09 / Track C)** in `packages/001.lore`.

Under the **Universal Negative-Control Law**, an architectural invariant or
pre-flight gate that has never been observed rejecting an impossible allocation
or invalid reference has not been proven to work. Use autonomous terminal
execution, file inspection, and static analysis tools to verify that somatic
capability arithmetic, reciprocal spatial linking, byte-0 file serialization,
curses menu integration, and compiler determinism are fully operational without
breaching workspace boundaries.

Do not modify functional code without reporting your findings first. Execute
terminal validation commands directly, inspect files, and provide an objective
**PASSED / FAILED** verification report.

---

### Phase 1: Boundary Integrity, Dependency Purity & Terminal Safety Audit

1. **Immutable Runner Boundary Check (`apps/995.library`):**

- Run:

```bash
git status -s apps/995.library/

```

- **Pass Condition:** Returns zero lines. Confirm that the upstream harness was
  not touched.

2. **Canon Corpus Immutability Check (`series/under-the-floorboards`):**

- Run:

```bash
git status -s series/under-the-floorboards/

```

- **Pass Condition:** Returns zero lines. Confirm that test fixtures did not
  dirty or overwrite the canonical baseline storyworld.

3. **External Prompt Library Purity:**

- Inspect `packages/001.lore/package.json`.

- Confirm that no external interactive prompt libraries (`@clack/prompts`,
  `inquirer`, `commander`, `prompts`) were installed.

- Confirm that all interactive flows strictly leverage native Blessed unit
  dispatches (`OPEN_CHOICE`, `OPEN_INPUT`, `ste.hunt`).

4. **Windows CMD 7-Bit ASCII Safety:**

- Run:

```bash
git grep -P "[^\x00-\x7F]" packages/001.lore/src/scaffolder/ packages/001.lore/00.lore.unit/ packages/001.lore/98.menu.unit/

```

- **Pass Condition:** Returns zero matches (exit code 1).

- Confirm that all terminal prompts, console logs (`cns00`), and telemetry
  banners use clean ASCII markers (`>>`, `[OK]`, `[FAIL]`, `[ONLINE]`, `::`,
  `---`) to prevent terminal character corruption in Windows shells.

---

### Phase 2: Static Code & Scaffolder Engine Inspection

1. **Somatic SAT Solver (`packages/001.lore/src/scaffolder/satSolver.ts`):**

- Inspect `evaluateSomaticSAT`:
- Verify limb capacity arithmetic: starts at 2.0 arms, drops to 0.0 for
  `sessile` or `slithering` baselines, and decrements by 1.0 for severe arm
  conditions (`severed_right_arm`, `paralyzed_arm`, etc.).
- Verify upper-limb saturation assertion: flags violations whenever
  $\text{ArmsRequired} > \text{ArmsAvailable}$.
- Verify scalar bounds: asserts that `escalation_ceiling` is an integer strictly
  bounded between 1 and 5.
- Verify kinetic prohibition consistency: asserts that `NO_SPRINT` motor
  limitation demands `sprint` or `run` in `banned_kinetic_verbs`.

2. **Reciprocal Spatial Link Reconciliation
   (`packages/001.lore/src/scaffolder/reciprocalLinker.ts`):**

- Inspect `reconcileReciprocalSpatialLinks`:
- Confirms it opens each target location file listed in `adjacentLocationIds`.
- Appends the new location ID to `adjacent_locations` without duplication.
- Confirms that the resulting file is saved atomically via `.tmp_${Date.now()}`
  and `fs.renameSync`.
- Confirms that the Byte-0 Frontmatter Law is preserved (`---` strictly at
  byte 0) on all mutated files.

3. **Two-Rail Entity Builder & Human Save Gate
   (`packages/001.lore/src/scaffolder/entityBuilder.ts`):**

- Inspect `buildByteZeroDossier`:
- Confirms the Machine Rail frontmatter is serialized using YAML starting at
  index 0.
- Confirms the Voice Rail stubs are appended below frontmatter
  (`## Wants (surface)`, `## Wants (actual)`, `## Blind spot`,
  `## Voice notes`).

- Inspect `openHumanSaveGate`:
- Confirms execution guards: immediately returns `false` if
  `!process.stdin.isTTY`, `process.env.CI`, or
  `process.env.NODE_ENV === 'test'`.
- Prevents headless CI hangs or child process deadlocks.

4. **Module Re-Exports:**

- Confirm `packages/001.lore/src/scaffolder/index.ts` exports `satSolver`,
  `reciprocalLinker`, and `entityBuilder`.
- Confirm `packages/001.lore/src/index.ts` re-exports `./scaffolder/index.js`.

---

### Phase 3: Unit Wire-Up & Blessed TUI Flow Audit

1. **Action & Reducer Handling (`packages/001.lore/00.lore.unit/`):**

- Verify `lore.action.ts` declares `SCAFFOLD_ENTITY` and the `ScaffoldEntity`
  action class.
- Verify `lore.buzzer.ts` re-exports `scaffoldEntity`.
- Verify `lore.reduce.ts` routes `Act.SCAFFOLD_ENTITY` through
  `Buzz.scaffoldEntity`.
- Verify `buz/lore.buzz.ts` executes `scaffoldEntityDossier`, triggers automatic
  re-compilation via `compileLoreInstance`, updates `lastStateHash`, and emits
  telemetry to `cns00`.

2. **Negative-Control Error Resolution:**

- In `scaffoldEntity` (`buz/lore.buzz.ts`), verify that invalid inputs or
  pre-flight SAT rejections:
- Set `cpy.lastCompileStatus = 'ERROR'`.
- Dispatch diagnostic logs to `cns00`.
- Resolve `bal.slv` safely with
  `{ lorBit: { idx: 'scaffold-entity-error', val: 0, dat: { error: ... } } }`
  rather than throwing an unhandled exception.

3. **Menu Flow Integration
   (`packages/001.lore/98.menu.unit/buz/00.menu.buzz.ts`):**

- Verify `'SCAFFOLD ENTITY'` is registered in the root menu options list.
- Verify selecting `'SCAFFOLD ENTITY'` opens a type selector choice list
  (`CHARACTER`, `LOCATION`, `GRIEVANCE`, `POSSESSION`, `CANCEL`).
- Verify selecting a type prompts for an entity slug via `OPEN_INPUT`, builds
  default template frontmatter, and dispatches `ActLor.SCAFFOLD_ENTITY`.
- Confirm that headless execution (`!lib || typeof lib.hunt !== 'function'`)
  returns safely without throwing null pointer exceptions.

---

### Phase 4: Automated Test Execution & Tripwire Verification

Execute the verification battery from the repository root:

```bash
# 1. Typecheck the 001.lore package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the dedicated entity authoring and scaffolder test suite
npm run test --workspace=@camp_candor/001.lore -- test/scaffold.test.ts

# 3. Run the complete lore test battery (10/10 test files must pass)
npm run test:lore

# 4. Assert zero drift against canonical snapshot
npm run audit:lore

# 5. Confirm immutable runner boundary remains pristine
git status -s apps/995.library/

```

**Pass Conditions:**

- `npm run build --workspace=@camp_candor/001.lore` exits with code 0 (`tsc -b`
  passes with zero errors).

- `test/scaffold.test.ts` passes all 6 tests:
- Somatic SAT solver rejects hand allocations exceeding available functional
  limbs.

- Somatic SAT solver rejects escalation ceilings outside [1, 5].

- Byte-0 Frontmatter Law guaranteed on generated dossiers
  (`startsAt(0) === '---'`).

- Reciprocal spatial linking establishes mutual adjacencies at byte 0.

- `scaffoldEntity` validates, persists, and automatically reseals canonical
  state ($H_{\text{root}}$).

- `scaffoldEntity` handles pre-flight SAT failure gracefully with `val: 0`.

- `npm run test:lore` passes all 10 test suites (58+ total tests green).

- `npm run audit:lore` exits with code 0 (zero drift on
  `compiled/under-the-floorboards/bible-state.json`).

- `git status -s apps/995.library/` returns 0 lines.

---

### Phase 5: Output Deliverable Format

Structure your report using the following standard deliverable format:

1. **Terminal Command Log:**

- Commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each capability as **PASSED** or **FAILED**:

- `Immutable Runner Boundary Confirmation (apps/995.library untouched)`

- `Canonical Corpus Immutability (series/under-the-floorboards clean)`

- `Zero New CLI / Runtime Dependencies`

- `ASCII-Safe Telemetry Formatting (zero cmd.exe emoji corruption)`

- `Pre-Flight Somatic SAT Solver Verified (Limbs, Slots, Escalation Bounds)`

- `Reciprocal Spatial Link Reconciliation Operational (Byte 0 Preserved)`

- `Two-Rail Entity Builder & Byte-0 Frontmatter Law Verified`

- `Human Save Gate Headless/TTY Protection Verified`

- `00.lore.unit SCAFFOLD_ENTITY Action & Buzzer Integrated`

- `98.menu.unit SCAFFOLD ENTITY Menu Option & Flow Operational`

- `Dedicated Scaffolder Test Battery Passed (test/scaffold.test.ts)`

- `Full Lore Test Battery Passed (10/10 test files, 58+ tests)`

- `Zero-Drift Canonical Tripwire Passed (npm run audit:lore)`

3. **Identified Violations & Immediate Patches:**

- If any check fails, state the exact file path, line number, and minimal patch
  required to achieve compliance.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Interactive Entity Scaffolder Engine
  Certified)** or **BLOCKED (Audit Gate Tripped)**.
