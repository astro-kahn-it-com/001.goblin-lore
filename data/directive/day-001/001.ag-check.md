# TASK: Antigravity Audit & Verification — Blessed Terminal Harness & Lore Engine Unit Integration

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## TARGET WORKSPACE: `packages/001.lore`

## AUDIT TARGETS:

## - `packages/001.lore/00.lore.unit/`

## - `packages/001.lore/98.menu.unit/`

## - `packages/001.lore/BEE.ts`

## - `packages/001.lore/test/lore.unit.test.ts`

## - `apps/995.library/` (Immutable Boundary Check)

You are an expert systems architect and autonomous QA verification
engineer[cite: 1]. Your mandate is to conduct a forensic audit of the newly
integrated **Blessed Terminal Harness UI Integration (`00.lore.unit` &
`98.menu.unit`)** in `packages/001.lore`[cite: 1, 3].

Under the **Universal Negative-Control Law**, a system component that has never
been observed rejecting an invalid input or operating under hostile headless
conditions has not been proven to work[cite: 1, 11]. Use autonomous terminal
execution, file inspection, and static analysis tools to verify that terminal
reactivity, curses grid math, headless resilience, and compilation determinism
remain intact without breaching workspace boundaries[cite: 1, 3, 13].

Do not modify functional code without reporting your findings first[cite: 1].
Execute terminal validation commands directly, inspect files, and provide an
objective **PASSED / FAILED** verification report[cite: 1].

---

### Phase 1: Boundary Integrity & Dependency Purity Audit

1. **Immutable Harness Boundary Confirmation (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** The command must return zero lines (completely clean,
     untouched, and uncommitted)[cite: 1].
   - Confirm that no new units, actions, reducers, or menu files were placed
     directly inside `apps/995.library/`[cite: 1].

2. **Dependency & Process Contention Purity:**
   - Inspect `packages/001.lore/package.json`[cite: 1].
   - Confirm that **zero external CLI prompt libraries** (`@clack/prompts`,
     `inquirer`, `commander`, `prompts`) were introduced[cite: 1, 3].
   - Confirm that execution relies strictly on existing internal bus primitives
     (`rx-lite`, `clone-deep`, `zod`, `gray-matter`) and Node.js built-ins
     (`node:fs`, `node:path`, `node:crypto`)[cite: 1, 3, 13].

3. **Windows CMD Character Encoding Safety:**
   - Run:
     ```bash
     git grep -P "[\x{1F300}-\x{1FAD6}]" packages/001.lore/00.lore.unit/ packages/001.lore/98.menu.unit/
     ```
   - **Pass Condition:** Returns 0 hits (exit code 1)[cite: 3].
   - Confirm that all console logs, telemetry lines, and menu labels use clean
     ASCII status markers (`>>`, `[OK]`, `[FAIL]`, `[ONLINE]`, `::`, `---`) to
     prevent terminal character corruption in Windows shells[cite: 1, 3, 7].

4. **Deterministic Ambient Time Purity:**
   - Inspect `packages/001.lore/00.lore.unit/buz/lore.buzz.ts`[cite: 1].
   - Confirm that state sealing and hash calculations do not evaluate
     `Date.now()`[cite: 1, 3].

---

### Phase 2: Unit Architecture & Wire-Up Audit

1. **Standard 7-File Unit Anatomy (`00.lore.unit`):**
   - Inspect `packages/001.lore/00.lore.unit/` and verify the presence and
     structural correctness of[cite: 1, 13]:
     - `fce/lore.bit.ts` (implements `LoreBit` interface with `idx`, `src`,
       `val`, `dat`, `slv`)[cite: 13].
     - `fce/lore.interface.ts` (implements `Lore` interface with `idx`,
       `lastStateHash`, `lastEntityCount`, `lastCompileStatus`)[cite: 13].
     - `lore.action.ts` (declares `INIT_LORE`, `COMPILE_LORE`, `AUDIT_LORE` and
       their Action classes)[cite: 13].
     - `lore.model.ts` (declares `LoreModel` defaults)[cite: 13].
     - `lore.reduce.ts` (uses `clone-deep` and maps actions to buzzers)[cite:
       13].
     - `lore.buzzer.ts` (re-exports `initLore`, `compileLore`,
       `auditLore`)[cite: 13].
     - `buz/lore.buzz.ts` (implements business logic and telemetry)[cite: 13].
     - `lore.unit.ts` (unit container class)[cite: 13].

2. **Package Registration (`packages/001.lore/BEE.ts`):**
   - Inspect `packages/001.lore/BEE.ts`[cite: 1].
   - Confirm that `list` exports `[LoreUnit, SeriesUnit, MenuUnit]`[cite: 13].
   - Confirm that `models` registers `lore`, `series`, and `menu`[cite: 13].
   - Confirm that `reducers` maps `loreReducer`, `seriesReducer`, and
     `menuReducer`[cite: 13].

3. **Repo Root Traversal Purity (`resolveRepoRoot`):**
   - Inspect `resolveRepoRoot()` in `00.lore.unit/buz/lore.buzz.ts`[cite: 3].
   - Confirm it climbs directory parents until both `packages` and (`series` or
     `package.json`) are present[cite: 3].
   - Verify it avoids falling back to an invalid directory or throwing an
     unhandled exception when invoked from nested paths[cite: 3].

---

### Phase 3: Headless Resilience & Terminal Telemetry Audit

Inspect `packages/001.lore/00.lore.unit/buz/lore.buzz.ts`:

1. **Headless Execution Guarding:**
   - Confirm that all interactions with the Blessed host runner
     (`global.LIBRARY.hunt`) are guarded by:
     ```typescript
     const lib = (global as any).LIBRARY
     if (lib && typeof lib.hunt === 'function') { ... }
     ```
   - Verify that compiling or auditing outside of the Blessed TUI (e.g., during
     Vitest runs or CLI execution) completes cleanly without throwing
     `TypeError: Cannot read properties of undefined`[cite: 3].

2. **Negative-Control Graceful Failure:**
   - Inspect the `catch` block of `compileLore`:
     - Confirm that targeting an invalid or missing series slug does **not**
       throw an unhandled exception[cite: 3].
     - Asserts that `cpy.lastCompileStatus` transitions to `'ERROR'`[cite: 3].
     - Asserts that `bal.slv` is invoked with
       `{ lorBit: { idx: 'compile-lore-error', val: 0, dat: { error: ... } } }`[cite:
       3].

3. **Telemetry Console Channel Integrity:**
   - Confirm that compiler status, state hashes ($H_{\text{root}}$), entity
     tallies, applied pardons, and epistemic advisories are dispatched
     specifically to console widget `cns00`[cite: 3, 13].

---

### Phase 4: Blessed Menu & 12x12 Grid Responsive Math Audit

Inspect `packages/001.lore/98.menu.unit/buz/00.menu.buzz.ts`:

1. **Menu Option Registry:**
   - Confirm the choice list contains:
     - `'COMPILE LORE INSTANCE'`[cite: 3]
     - `'INSPECT CANON BIBLE'`[cite: 3]
     - `'AUDIT CORPUS INTEGRITY'`[cite: 3]
     - `'ROOT MENU'`[cite: 3]

2. **Dynamic Series Sweeping & Submenu Sizing:**
   - Under `case 'COMPILE LORE INSTANCE'`:
     - Confirm it reads `<repoRoot>/series/` dynamically via `fs.readdirSync`
       rather than relying on a static array[cite: 3].
     - Confirm it calculates dynamic grid height:
       ```typescript
       ySpan: Math.min(12, Math.max(6, subChoices.length + 2))
       ```
     - Confirm selecting a series slug dispatches
       `ste.hunt(ActLor.COMPILE_LORE, { src: chosenSlug })`[cite: 3].

3. **Screen Layout Math Allocation:**
   - Verify the navigation menu allocates grid coordinates:
     `x: 0, y: 4, xSpan: 4, ySpan: 8`[cite: 3, 13].
   - Verify the primary console stream `cns00` allocates the right-hand panel:
     `x: 4, y: 0, xSpan: 8, ySpan: 12`[cite: 3, 13].

---

### Phase 5: Automated Test Execution & Tripwire Verification

Execute the verification battery from the repository root:

```bash
# 1. Typecheck the 001.lore package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the dedicated lore unit test suite
npm run test --workspace=@camp_candor/001.lore -- test/lore.unit.test.ts

# 3. Run all unit, golden, linter, pardon, epistemic, and harness test suites (9/9 suites must pass)
npm run test:lore

# 4. Execute the canonical zero-drift tripwire
npm run audit:lore

# 5. Confirm immutable runner boundary remains pristine
git status -s apps/995.library/

```

**Pass Conditions:**

- `npm run build --workspace=@camp_candor/001.lore` exits with code 0 (`tsc -b`
  clean).

- `test/lore.unit.test.ts` passes all 5 tests:
- Root directory resolution.

- `INIT_LORE` state transition.

- Headless compilation of `series/under-the-floorboards` (capturing 64-character
  SHA-256 hash).

- Graceful negative-control error resolution (`val: 0`) on invalid series.

- In-memory inspection and disk audit of canonical bible state.

- `npm run test:lore` passes all 9 test suites (52+ tests passing with zero
  failures).

- `npm run audit:lore` exits with code 0 (zero drift on
  `compiled/under-the-floorboards/bible-state.json`).

- `git status -s apps/995.library/` returns 0 lines.

---

### Phase 6: Output Deliverable Format

Format your audit report using the following structure:

1. **Terminal Command Log:**

- Commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each capability as **PASSED** or **FAILED**:

- `Immutable Runner Boundary Confirmation (apps/995.library untouched)`

- `Zero New CLI / Runtime Dependencies`

- `ASCII-Safe Telemetry Formatting (zero cmd.exe emoji corruption)`

- `Deterministic Ambient Time Purity in Buzzers`

- `Standard 7-File Unit Anatomy Implemented (00.lore.unit)`

- `Full Unit Registration in packages/001.lore/BEE.ts`

- `Headless Safety & Defensive global.LIBRARY Guarding`

- `Negative-Control Graceful Failure on Missing Series (val: 0)`

- `Dynamic Series Discovery & 12x12 Grid Math in 98.menu.unit`

- `Dedicated Lore Unit Suite Passed (test/lore.unit.test.ts)`

- `Full Lore Test Battery Passed (9/9 suites, 52+ tests)`

- `Zero-Drift Canonical Tripwire Passed (npm run audit:lore)`

3. **Identified Violations & Immediate Patches:**

- If any check fails, state the exact file path, line number, and minimal patch
  required to resolve it.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Terminal Harness UI Integration Certified)** or
  **BLOCKED (Audit Gate Tripped)**.

```

```
