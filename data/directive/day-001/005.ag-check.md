# TASK: Antigravity Audit & Verification — Canonical Ensemble & Multi-Tier Grievance DAG (EPIC-10)

## REPOSITORY: `001.goblin-lore` (or monorepo root)

## TARGET WORKSPACE: `series/under-the-floorboards` & `packages/001.lore`

## AUDIT TARGETS:

## - `series/under-the-floorboards/characters/` (wart.md, mum_grissel.md, bog.md, spleen.md)

## - `series/under-the-floorboards/locations/` (the_warm_pipe.md, the_pantry_wall.md, the_splinter_gap.md, the_attic_stair.md)

## - `series/under-the-floorboards/possessions/` (copper_flange.md, silver_spoon.md)

## - `series/under-the-floorboards/grievances/` (7 total grievance dossiers)

## - `series/under-the-floorboards/ontology/unresolved.json`

## - `compiled/under-the-floorboards/bible-state.json`

## - `apps/995.library/` (Immutable Boundary Check)

You are an expert systems architect and autonomous QA verification
engineer[cite: 1, 4]. Your mandate is to execute an exhaustive forensic audit of
the expanded canonical storyworld **`under-the-floorboards`** following the
implementation of **EPIC-10** (Canonical Ensemble & Multi-Tier Grievance DAG
Expansion)[cite: 4].

Under the **Universal Negative-Control Law**, a relational link, somatic
capability gate, or epistemic boundary that has never been observed rejecting an
ungrounded allocation or cyclic conflict has not been proven to work[cite: 1, 5,
7]. Use autonomous terminal execution, file inspection, and static analysis
tools to verify that somatic physical invariants, reciprocal spatial topology,
complete dyadic coverage, DAG acyclicity, and cryptographic compilation
determinism are fully upheld without breaching workspace boundaries[cite: 1, 2,
4].

Do not modify functional code or authored canon without reporting your findings
first[cite: 1, 4]. Execute terminal validation commands directly, inspect files,
and provide an objective **PASSED / FAILED** verification report[cite: 1, 4].

---

### Phase 1: Boundary Integrity, Git Hygiene & Encoding Audit

1. **Immutable Harness Fence (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** Returns zero lines (exit code 0)[cite: 1, 4]. Confirm
     that the host runner was not modified or contaminated[cite: 1, 4].

2. **Byte-0 Frontmatter Law Verification:**
   - Sweep all `.md` dossiers across `series/under-the-floorboards/` using
     hex/byte inspection:
     ```bash
     node -e "
     const fs = require('fs');
     const path = require('path');
     const dirs = ['characters', 'locations', 'possessions', 'grievances'];
     let failed = false;
     dirs.forEach(d => {
       const dirPath = path.join('series/under-the-floorboards', d);
       if (!fs.existsSync(dirPath)) return;
       fs.readdirSync(dirPath).filter(f => f.endsWith('.md')).forEach(f => {
         const buf = fs.readFileSync(path.join(dirPath, f));
         if (buf.length < 3 || buf[0] !== 0x2D || buf[1] !== 0x2D || buf[2] !== 0x2D) {
           console.error('BYTE-0 VIOLATION:', path.join(dirPath, f));
           failed = true;
         }
       });
     });
     if (failed) process.exit(1);
     console.log('>> [BYTE-0 CHECK] All Markdown dossiers start strictly at byte 0.');
     "
     ```
   - **Pass Condition:** Zero files violate byte 0 (`---`)[cite: 2, 13]. Zero
     leading newlines, whitespace, or UTF-8 BOM markers (`0xEF 0xBB 0xBF`)[cite:
     13].

3. **Windows CMD 7-Bit ASCII Safety:**
   - Run:
     ```bash
     git grep -P "[^\x00-\x7F]" series/under-the-floorboards/
     ```
   - **Pass Condition:** Returns zero matches (exit code 1). Confirms all
     authored files adhere strictly to 7-bit ASCII status markers and
     punctuation[cite: 2].

---

### Phase 2: Ensemble Roster & Somatic SAT Inspection

1. **Ensemble Quartet Verification ($N = 4$):**
   - Confirm the presence and schema validity of all 4 characters:
     - `char_bog` (`characters/bog.md`)
     - `char_spleen` (`characters/spleen.md`)
     - `char_wart` (`characters/wart.md`)
     - `char_mum_grissel` (`characters/mum_grissel.md`)

2. **Wart Somatic Invariant & SAT Evaluation:**
   - Inspect `characters/wart.md`:
     - Locomotion: `bipedal_hunched`[cite: 2].
     - Condition: `severed_left_arm`[cite: 2, 4].
     - SAT Upper-Limb Equation: Asserts
       $\text{ArmsAvailable} = 2.0 - 1.0 = 1.0$[cite: 6].
     - Inventory Allocation: Asserts `held: ['item_copper_flange']` demands
       exactly 1.0 hand, satisfying
       $\text{ArmsRequired} \le \text{ArmsAvailable}$[cite: 2, 6].
     - Kinetic Consistency: Asserts `banned_kinetic_verbs` contains `sprint`,
       `vault`, `climb`, and `embrace`[cite: 2].
     - Epistemic Ceiling: Integer scalar $\le 4$[cite: 1, 2].

3. **Mum Grissel Somatic & Institutional Invariant:**
   - Inspect `characters/mum_grissel.md`:
     - Locomotion: `limping`[cite: 2].
     - Condition: `stiff_left_knee`[cite: 1, 2].
     - Kinetic Consistency: Asserts `banned_kinetic_verbs` contains `sprint`,
       `leap`, `vault`, and `scramble`[cite: 1, 2].
     - Institutional Brake: Asserts `escalation_ceiling: 2` (presence
       mechanically bounds domestic bickering)[cite: 1, 2].
     - Cache Allocation: Confirms cached emergency items (`item_copper_flange`,
       `item_silver_spoon`) resolve cleanly to existing possession IDs[cite: 1,
       2].

---

### Phase 3: Spatial Topology & Reciprocal Link Reconciliation

1. **Spatial Nodes Inventory:**
   - Confirm presence of 4 distinct location nodes[cite: 2]:
     - `loc_the_warm_pipe` (`locations/the_warm_pipe.md`)
     - `loc_the_pantry_wall` (`locations/the_pantry_wall.md`)
     - `loc_the_splinter_gap` (`locations/the_splinter_gap.md`)
     - `loc_the_attic_stair` (`locations/the_attic_stair.md`)

2. **Reciprocal Adjacency Graph Audit:**
   - Verify that every spatial connection is bidirectional:
     - `the_warm_pipe` $\longleftrightarrow$ `the_pantry_wall` &
       `the_splinter_gap`
     - `the_pantry_wall` $\longleftrightarrow$ `the_warm_pipe` &
       `the_attic_stair`
     - `the_splinter_gap` $\longleftrightarrow$ `the_warm_pipe` &
       `the_attic_stair`
     - `the_attic_stair` $\longleftrightarrow$ `the_pantry_wall` &
       `the_splinter_gap`
   - **Pass Condition:** If node $A$ lists $B$ in `adjacent_locations`, node $B$
     must explicitly list $A$ in its `adjacent_locations`. Zero one-way spatial
     traps permitted.

---

### Phase 4: Dyadic Coverage & Multi-Tier Grievance DAG Audit

1. **Dyadic Pairwise Coverage Verification:**
   - Verify that all $\frac{N(N-1)}{2} = \frac{4 \times 3}{2} = 6$ dyadic
     channels possess $\ge 1$ active primary grievance[cite: 4, 9]:
     - [ ] Channel 1: `char_bog` $\longleftrightarrow$ `char_spleen`
           (`resource_hoarding.md`)[cite: 4, 9]
     - [ ] Channel 2: `char_spleen` $\longleftrightarrow$ `char_wart`
           (`watch_rotation_slack.md`)[cite: 4, 9]
     - [ ] Channel 3: `char_wart` $\longleftrightarrow$ `char_bog`
           (`boundary_scouting.md`)[cite: 4, 9]
     - [ ] Channel 4: `char_mum_grissel` $\longleftrightarrow$ `char_spleen`
           (`rations_distribution.md`)[cite: 4, 9]
     - [ ] Channel 5: `char_mum_grissel` $\longleftrightarrow$ `char_wart`
           (`matriarch_vigilance.md`)[cite: 4, 9]
     - [ ] Channel 6: `char_mum_grissel` $\longleftrightarrow$ `char_bog`
           (`hoarding_reveal.md` / `generational_famine_feud.md`)[cite: 4, 9]
   - **Pass Condition:** Zero dyadic channels report starvation. Every character
     pair has an eligible conflict lottery[cite: 4, 9].

2. **Multi-Tier Escalation DAG Structure ($\delta = 0 \to 1 \to 2$):**
   - Trace the escalation chain:
     - **Depth 0:** `resource_hoarding`
       $\xrightarrow{\text{spawns\_on\_max\_escalation}}$
       `hoarding_reveal`[cite: 4, 7]
     - **Depth 1:** `hoarding_reveal`
       $\xrightarrow{\text{spawns\_on\_max\_escalation}}$
       `generational_famine_feud`[cite: 4, 7]
     - **Depth 2:** `generational_famine_feud`
       $\xrightarrow{\text{spawns\_on\_max\_escalation}}$ terminal or mystery
       hook[cite: 4, 7]
   - Run Depth-First Search (DFS) 3-color traversal across all grievances in
     `series/under-the-floorboards/grievances/`:
     - Assert graph acyclicity: confirm that no escalation path cycles back onto
       an active ancestor node ($A \to B \to A$ is completely blocked)[cite: 2,
       4, 6].

3. **The 4-Outcome Scene Craft Mandate:**
   - Inspect all 7 grievance files (`resource_hoarding`, `watch_rotation_slack`,
     `boundary_scouting`, `rations_distribution`, `matriarch_vigilance`,
     `hoarding_reveal`, `generational_famine_feud`).
   - Confirm that each file explicitly defines non-empty string descriptors for
     all four duel outcomes[cite: 1, 7]:
     - `outcomes.clean_win`[cite: 1, 7]
     - `outcomes.costly_compromise`[cite: 1, 7]
     - `outcomes.mutual_wound`[cite: 1, 7]
     - `outcomes.catastrophic_backfire`[cite: 1, 7]

---

### Phase 5: Instance Epistemic Registry & Disjointness Audit

1. **Instance Registry Inspection
   (`series/under-the-floorboards/ontology/unresolved.json`):**
   - Confirm the existence of
     `series/under-the-floorboards/ontology/unresolved.json`[cite: 2].
   - Confirm `mystery_the_famine` maps all 4 active ensemble members[cite: 2]:
     - `char_bog`: `'TOTAL_IGNORANCE'`[cite: 2]
     - `char_spleen`: `'RUMOR_ONLY'` (with explicit non-empty
       `distorted_beliefs`)[cite: 2]
     - `char_wart`: `'FALSE_BELIEF'` (with explicit non-empty
       `distorted_beliefs`)[cite: 2]
     - `char_mum_grissel`: `'WITHHELD'` (dramatic restraint directive)[cite: 2]

2. **Foreign-Key & Refinement Hard Gates:**
   - Confirm that all character keys in `epistemic_horizons` and
     `distorted_beliefs` resolve against existing characters in
     `series/under-the-floorboards/characters/`[cite: 1].
   - Assert that `DistortedBeliefStringSchema` contains zero prompt-injection
     square brackets (`[` or `]`)[cite: 1].
   - Confirm that `permitted_clue_tokens` and flattened `trigger_lemma_sets` are
     strictly disjoint ($A \cap B = \emptyset$)[cite: 1].

---

### Phase 6: Automated Test Execution, Resealing & Zero-Drift Verification

Execute the complete verification battery from the repository root:

```bash
# 1. Typecheck the lore package
npm run build --workspace=@camp_candor/001.lore

# 2. Run the full unit, golden, linter, pardon, epistemic, and scaffolder test battery
npm run test:lore

# 3. Compile the expanded storyworld to verify Passes 1-3
npx tsx packages/001.lore/src/compiler.ts

# 4. Assert total processed entity count matches expanded corpus (4 characters + 4 locations + 2 possessions + 7 grievances = 17 entities)
node -e "
const state = JSON.parse(require('fs').readFileSync('compiled/under-the-floorboards/bible-state.json', 'utf8'));
const counts = {
  characters: Object.keys(state.entities.characters || {}).length,
  locations: Object.keys(state.entities.locations || {}).length,
  possessions: Object.keys(state.entities.possessions || {}).length,
  grievances: Object.keys(state.entities.grievances || {}).length,
};
const total = Object.values(counts).reduce((a, b) => a + b, 0);
console.log('>> [INDEXED ENTITIES]', counts, 'TOTAL:', total);
if (total < 17) {
  console.error('ERROR: Expected at least 17 indexed entities in bible-state.json');
  process.exit(1);
}
"

# 5. Execute the canonical zero-drift tripwire
npm run audit:lore

# 6. Verify immutable harness boundary remains pristine
git status -s apps/995.library/

```

**Pass Conditions:**

- `npm run build --workspace=@camp_candor/001.lore` exits with code 0 (`tsc -b`
  clean).

- `npm run test:lore` passes all 10 test files (including series tests updated
  for expanded counts).

- Compiled `bible-state.json` contains $\ge 17$ entities with a valid
  64-character SHA-256 state seal ($H_{\text{root}}$).

- `npm run audit:lore` exits with code 0 (zero git diff on
  `compiled/under-the-floorboards/bible-state.json`).

- `git status -s apps/995.library/` returns 0 lines.

---

### Phase 7: Output Deliverable Format

Format your audit findings using the following structure:

1. **Terminal Command Log:**

- Commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each requirement as **PASSED** or **FAILED**:

- `Immutable Runner Boundary (apps/995.library untouched)`

- `Byte-0 Frontmatter Law Verified across all 17 dossiers`

- `ASCII-Safe Telemetry & Dossier Formatting (zero UTF-8 emojis)`

- `Canonical Ensemble Quartet Completed (Bog, Spleen, Wart, Mum Grissel)`

- `Wart Somatic Invariant Satisfied (severed arm, ArmsAvailable = 1.0, SAT passed)`

- `Mum Grissel Somatic & Escalation Ceiling Bound (ceiling = 2, limping)`

- `Reciprocal Spatial Topology Complete (4 nodes mutually adjacent)`

- `Dyadic Pairwise Coverage Verified (all 6 channels active)`

- `Multi-Tier Grievance DAG Acyclicity Confirmed (Depth 0 -> 1 -> 2, DFS cycle-free)`

- `4-Outcome Scene Craft Mandate Satisfied across all 7 grievances`

- `Instance Epistemic Registry Bound (mystery_the_famine across 4 characters)`

- `Full Lore Test Battery Passed (npm run test:lore)`

- `Total Entity Count Threshold Verified (>= 17 entities)`
- `Canonical Zero-Drift Tripwire Passed (npm run audit:lore)`

3. **Identified Violations & Remediation Patches:**

- If any check fails, state the exact file path, line number, and minimal patch
  required to resolve it.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Canonical Ensemble & Multi-Tier DAG Certified)**
  or **BLOCKED (Audit Gate Tripped)**.

```

```
