# TASK: Antigravity Audit & Verification — Typed Downstream Ingestion Client & Somatic HUD (EPIC-11)

## REPOSITORY: `001.goblin-lore` (or monorepo root)[cite: 4]

## TARGET WORKSPACE: `packages/001.lore`[cite: 4, 11]

## AUDIT TARGETS:

## - `packages/001.lore/src/client.ts`

## - `packages/001.lore/src/index.ts`

## - `packages/001.lore/00.lore.unit/lore.action.ts`

## - `packages/001.lore/00.lore.unit/lore.buzzer.ts`

## - `packages/001.lore/00.lore.unit/buz/lore.buzz.ts`

## - `packages/001.lore/00.lore.unit/lore.reduce.ts`

## - `packages/001.lore/98.menu.unit/buz/00.menu.buzz.ts`

## - `packages/001.lore/test/client.test.ts`

## - `apps/995.library/` (Immutable Boundary Check)[cite: 4, 5]

You are an expert systems architect and autonomous QA verification
engineer[cite: 5, 8]. Your mandate is to conduct a forensic audit of the newly
integrated **Typed Downstream Ingestion Client (`src/client.ts`)** and its
**Blessed Terminal Interface Integration (`00.lore.unit` and
`98.menu.unit`)**[cite: 5, 14].

Under the **Universal Negative-Control Law**, a physical capability gate or
immutability boundary that has never been observed rejecting an illegal
mutation, unindexed character, or invalid kinetic action has not been proven to
work[cite: 5, 11, 18]. Use autonomous terminal execution, file inspection, and
static analysis tools to verify that somatic physical projections, deep-frozen
immutability, epistemic prompt directives, and Blessed TUI telemetry are fully
operational without breaching repository guardrails[cite: 5, 14].

Do not modify functional code or canonical dossiers without reporting your
findings first[cite: 5, 14]. Execute terminal validation commands directly,
inspect files, and provide an objective **PASSED / FAILED** verification
report[cite: 5, 8].

---

### Phase 1: Boundary Integrity, Git Hygiene & Encoding Audit

1. **Immutable Harness Boundary Fence (`apps/995.library`):**
   - Run:
     ```bash
     git status -s apps/995.library/
     ```
   - **Pass Condition:** Returns exactly 0 lines (exit code 0)[cite: 4, 5].
     Confirm the upstream terminal harness contains zero modifications or
     untracked scratch typings[cite: 4, 5].

2. **Canon Corpus & Compiled Snapshot Immutability:**
   - Run:
     ```bash
     git status -s series/under-the-floorboards/ compiled/under-the-floorboards/
     ```
   - **Pass Condition:** Returns exactly 0 lines[cite: 5]. Confirm that
     introducing client projections did not overwrite or drift canonical
     files[cite: 5].

3. **Zero New CLI / Runtime Dependencies:**
   - Inspect `packages/001.lore/package.json`[cite: 4, 5].
   - Confirm zero additions to `dependencies` or `devDependencies`[cite: 5].
     Operations must rely strictly on existing modules (`zod`, `clone-deep`,
     `gray-matter`, `rx-lite`) and Node.js built-ins (`node:fs`,
     `node:path`)[cite: 5, 7].

4. **Deterministic Ambient Time Purity:**
   - Run:
     ```bash
     git grep "Date.now()" packages/001.lore/src/client.ts
     ```
   - **Pass Condition:** Returns 0 hits (exit code 1)[cite: 5]. Client
     projection accessors and math solvers must never evaluate system
     clocks[cite: 5].

5. **Windows CMD 7-Bit ASCII Safety:**
   - Run:
     ```bash
     git grep -P "[^\x00-\x7F]" packages/001.lore/src/client.ts packages/001.lore/test/client.test.ts packages/001.lore/00.lore.unit/ packages/001.lore/98.menu.unit/
     ```
   - **Pass Condition:** Returns 0 matches (exit code 1)[cite: 5]. Confirms
     telemetry strings, types, and console formatters use clean ASCII tokens
     (`>>`, `[OK]`, `[FAIL]`, `::`, `---`) without multi-byte emojis[cite: 4,
     5].

---

### Phase 2: Static Code & API Surface Inspection

1. **Defensive Recursive Immutability (`deepFreeze`):**
   - Inspect `packages/001.lore/src/client.ts`.
   - Verify that `deepFreeze<T>(obj: T)` recursively seals objects and nested
     arrays via `Object.freeze()`[cite: 14].
   - Confirm that all exported projection helpers (`loadBibleState`,
     `getCharacterSomaticMask`, `getEpistemicDirectives`,
     `getStaticSpatialGraph`, `getActiveGrievanceDAG`) wrap their returns in
     `deepFreeze()`[cite: 14].

2. **Artifact Verification & Normalization (`loadBibleState`):**
   - Confirm dynamic monorepo root discovery via `resolveRepoRoot()` climbing
     upward to `packages` and (`series` or `package.json`).
   - Confirm `loadBibleState` asserts that `_meta.state_hash` is a 64-character
     hex string, throwing `[LORE CLIENT FATAL]` if corrupted or uncompiled[cite:
     14].
   - Confirm normalization handles both root `state.characters` and nested
     `state.entities.characters` schemas safely.

3. **Somatic Projection Logic (`getCharacterSomaticMask`):**
   - Confirm that `availableArms` initializes to `2.0` (or `0.0` for `sessile` /
     `slithering` baselines)[cite: 5].
   - Confirm deduction of `1.0` per severe arm condition
     (`braxial_plexus_severed`, `severed_right_arm`, `severed_left_arm`,
     `paralyzed_arm`, `amputated_limb`) with `Math.max(0.0, ...)` clamping[cite:
     5, 11].
   - Confirm querying an unindexed character ID throws an explicit fail-closed
     exception: `[LORE CLIENT] Unknown character ID: '...'`[cite: 5, 14].

4. **Epistemic & Spatial Projections:**
   - Confirm `getEpistemicDirectives` routes through
     `buildCharacterEpistemicPrompt` applying _Ignorance by Omission_ (total
     secrecy for unrevealed mysteries, `[UNVERIFIED HEARSAY]` for distorted
     beliefs, `[RESTRAINED KNOWLEDGE]` for withheld topics)[cite: 4, 5].
   - Confirm `getStaticSpatialGraph` extracts `adjacentLocations`,
     `acousticDampingFactor`, and `lightingLevel`[cite: 11, 14].
   - Confirm `getActiveGrievanceDAG` maps `spawns_on_max_escalation` target
     IDs[cite: 14].

5. **Package Barrel Export:**
   - Inspect `packages/001.lore/src/index.ts`.
   - Confirm `export * from './client.js'` is present[cite: 4, 14].

---

### Phase 3: Blessed Terminal Harness & Action Dispatch Audit

1. **Lore Unit Action & Reducer Mounting:**
   - Inspect `packages/001.lore/00.lore.unit/lore.action.ts`: verify
     `INSPECT_SOMATIC = '[Lore action] Inspect Somatic'` and `InspectSomatic`
     class.
   - Inspect `packages/001.lore/00.lore.unit/lore.reduce.ts`: confirm
     `Act.INSPECT_SOMATIC` routes to `Buzz.inspectSomatic` through
     `clone-deep`[cite: 4].

2. **Telemetry Streaming (`00.lore.unit/buz/lore.buzz.ts`):**
   - Confirm `inspectSomatic`:
     - Reads `seriesSlug` and `characterId` from action payload[cite: 5].
     - Loads state via `loadBibleState` and projects mask via
       `getCharacterSomaticMask`[cite: 5].
     - Safely checks
       `global.LIBRARY && typeof global.LIBRARY.hunt === 'function'` before
       dispatching UI updates (preventing test runner crashes)[cite: 4, 5].
     - Dispatches formatted ASCII lines to console widget `cns00`:
       - `>> [SOMATIC PROJECTION] <charId> (Gait: <locomotionBaseline>)`
       - `>> AVAILABLE ARMS: <availableArms> / 2.0 (Conditions: <conditions>)`
       - `>> MOTOR LIMITATIONS: [<motorLimitations>]`
       - `>> BANNED VERBS: <bannedKineticVerbs>`

3. **Menu Flow Integration (`98.menu.unit/buz/00.menu.buzz.ts`):**
   - Inspect `updateMenu`:
     - Confirm `'INSPECT SOMATIC PROFILE'` is added to the main menu choice
       array.
     - Confirm selecting it opens character choice list (`OPEN_CHOICE`) using
       available character IDs from
       `compiled/under-the-floorboards/bible-state.json`.
     - Confirm selecting an actor dispatches
       `ste.hunt(ActLor.INSPECT_SOMATIC, ...)`.

---

### Phase 4: Universal Negative-Control Runtime Verification

Execute a standalone Node.js negative-control verification script to confirm
mutation rejections and error handling:

```bash
node -e "
const path = require('path');
const { loadBibleState, getCharacterSomaticMask } = require('./packages/001.lore/dist/src/client.js');

let caughtTopLevel = false;
let caughtNested = false;
let caughtUnknownChar = false;
let caughtMaskMutation = false;

const state = loadBibleState('under-the-floorboards');

// 1. Top-level mutation rejection
try {
  state.entities.characters.char_bog.name = 'TAMPERED';
} catch (e) {
  if (e instanceof TypeError) caughtTopLevel = true;
}

// 2. Nested array mutation rejection
try {
  state.entities.characters.char_bog.logistical.held.push('item_injected');
} catch (e) {
  if (e instanceof TypeError) caughtNested = true;
}

// 3. Unknown character fail-closed rejection
try {
  getCharacterSomaticMask(state, 'char_phantom_ghost');
} catch (e) {
  if (e.message.includes('[LORE CLIENT] Unknown character ID')) caughtUnknownChar = true;
}

// 4. Projected mask mutation rejection
const mask = getCharacterSomaticMask(state, 'char_wart');
try {
  mask.availableArms = 2.0;
} catch (e) {
  if (e instanceof TypeError) caughtMaskMutation = true;
}

console.log('Negative Control Results:', {
  caughtTopLevel,
  caughtNested,
  caughtUnknownChar,
  caughtMaskMutation
});

if (!caughtTopLevel || !caughtNested || !caughtUnknownChar || !caughtMaskMutation) {
  console.error('NEGATIVE CONTROL FAILED: Immutability or fail-closed gates bypassed!');
  process.exit(1);
}

console.log('>> [NEGATIVE CONTROL PASSED] All mutation and boundary attacks rejected.');
"

```

- **Pass Condition:** Script outputs all four flags as `true` and exits with
  code 0.

---

### Phase 5: Automated Test Execution, Build & Zero-Drift Tripwire

Run the complete verification battery from the repository root:

```bash
# 1. Typecheck and build packages/001.lore
npm run build --workspace=@camp_candor/001.lore

# 2. Run dedicated client test suite
npm run test --workspace=@camp_candor/001.lore -- test/client.test.ts

# 3. Run full lore test battery (11 test suites)
npm run test:lore

# 4. Assert zero drift against canonical snapshot
npm run audit:lore

# 5. Confirm immutable runner boundary remains clean
git status -s apps/995.library/

```

**Pass Conditions:**

- `npm run build --workspace=@camp_candor/001.lore` exits with code 0 (`tsc -b`
  passes clean).

- `test/client.test.ts` passes all 11 unit tests:
- Monorepo root directory resolution.
- State loading with valid 64-character SHA-256 seal.
- Deep-freeze immutability rejecting mutations on names, held arrays, and
  metadata (`TypeError`).
- `char_wart`: `availableArms === 1.0` under condition `severed_left_arm`,
  negative verb mask populated.
- `char_mum_grissel`: `availableArms === 2.0`, `limping` gait, negative verb
  mask populated.
- `char_bog`: standard bipedal `availableArms === 2.0`.
- Unindexed character query throwing fail-closed error.
- Mutual spatial graph extraction with acoustic damping factors.
- Multi-tier grievance DAG extraction.
- Epistemic prompt directives applying _Ignorance by Omission_.
- `INSPECT_SOMATIC` reducer execution in headless mode.

- `npm run test:lore` passes all 11 test files (69+ total tests green).

- `npm run audit:lore` exits with code 0 (zero git diff on
  `compiled/under-the-floorboards/bible-state.json`).

- `git status -s apps/995.library/` returns 0 lines.

---

### Phase 6: Output Deliverable Format

Format your audit findings using the standard reporting structure:

1. **Terminal Command Log:**

- Commands executed, exit codes, and stdout/stderr summaries.

2. **Acceptance Criteria Verification Matrix:**

- A Markdown table grading each requirement as **PASSED** or **FAILED**:
- `Immutable Runner Boundary (apps/995.library untouched)`

- `Canonical Storyworld Immutability (series/ & compiled/ clean)`

- `Zero New Dependencies in package.json`

- `Deterministic Ambient Time Purity (zero Date.now() in client)`

- `ASCII-Safe Telemetry & Types (zero UTF-8 emojis)`

- `Recursive Immutability Verified (deepFreeze throws TypeError)`

- `64-Character SHA-256 Seal Validation on Load`

- `Somatic Capability Projection (Wart: 1.0 Arm, Mum Grissel: Limping)`

- `Fail-Closed Unknown Character Handling`

- `Ignorance by Omission Epistemic Prompt Projection`

- `Spatial Topology & Acoustic Damping Factor Projection`

- `Multi-Tier Grievance Escalation DAG Projection`

- `Blessed HUD Action Mounting & cns00 Streaming (00.lore.unit)`

- `Blessed Menu Integration (98.menu.unit INSPECT SOMATIC PROFILE)`

- `Dedicated Client Test Suite Passed (test/client.test.ts)`

- `Full Lore Test Battery Passed (11/11 test files, 69+ tests)`

- `Canonical Zero-Drift Tripwire Passed (npm run audit:lore)`

3. **Identified Violations & Remediation Patches:**

- If any check fails, state the exact file path, line number, and minimal patch
  required to achieve compliance.

4. **Final Architectural Verdict:**

- Explicitly declare: **PASSED (Typed Downstream Ingestion Client & Somatic HUD
  Certified)** or **BLOCKED (Audit Gate Tripped)**.

```

```
