export const initAlligator = (
    cpy: AlligatorModel,
    bal: AlligatorBit,
    ste: State,
) => {
    debugger
    return cpy
}

export const updateAlligator = (
    cpy: AlligatorModel,
    bal: AlligatorBit,
    ste: State,
) => {
    return cpy
}

import type { AlligatorModel } from '../alligator.model'
import type AlligatorBit from '../fce/alligator.bit'
import type State from '../../99.core/state'
